# ONNX 计算图极限压缩实战手册

> **适用场景**：给定一批网格变换任务（输入输出均为 ≤30×30、取值 0–9 的离散网格），为每个任务单独构建一个 ONNX 网络复现该变换，并把网络的"成本"压到最低。
> **单题计分**：`score = max(1, 25 − ln(cost))`，`cost = 参数总数 + 中间张量累计字节数`。全部任务求和即总分。任何一题被判错 → 该题 0 分。
> **使用方式**：这不是一篇从头读到尾的教程，而是一本**攻坚参考手册**。第 0/1/2 章必须通读并全程记住；第 3–5 章按需跳转查阅。
> **前提**：一个攻坚窗口只处理**一道题**，时间不限，目标是把这道题的 cost 压到你能构建并实测出来的最低点。

---

# 第 0 章 攻坚纪律（先读这一章，全程保持）

## 0.1 禁用结论词

**这份手册的存在意义是突破极限。任何"这题已经压到底了"的表述都被禁止**——因为在这类任务上，这种表述每一次被使用时都被后续事实推翻了。

★以下概念禁止书写、禁止思考、禁止作为对某道题的结论★：

- 「地板 / 触底 / 成本下限 / 硬下限 / 理论最小值 / 真实复杂度下限」
- 「已最优 / 最优解 / 可证明最小 / 不可能更低」
- 「不可压缩 / 压不动了 / 降不下去了」
- 「到头了 / 见底了 / 没救了」**作为对任务的判决**（作为目标动词使用是允许的，见 0.4）
- 「内容相关性太强 / 规律太难」**作为放弃的理由**（描述"规律难发现"是事实陈述，可以；把它当停止信号，不行）
- 给任何一个具体的 cost 数字盖上「这就是最小值 / 这就是下界 / 到此为止」的戳

★为什么这是红线（背下来）★：这些词会让一个攻坚窗口**提前退场**。实践反复否定它们：

- 一道被隐性当作"不可攻"的百万级 cost 基线，经过**从规律重写 + 持续无损压缩**，降到万级，压缩约三个数量级，且全样本逐样精确——纯粹靠**重新编码同一个函数**，没有改变任何答案。
- 多次独立的"这就是最小值"判定，都不是靠运气、而是靠**实际构建出来并用成本度量工具量过**的更便宜编码被推翻：某题"5×5 真实最小值"被分组卷积死通道收缩打破；某题"单卷积已是极限"被固定小网格裁剪重建打破；某题"已到位"被删掉一个从不生效的负偏置打破。每一次都是在一个已被宣布为最终的 cost 上又拿到了无损胜利。

## 0.2 「地板」只能是构建实测的产物，绝不是分析结论

★不只是某一种判定标准下的地板是幻觉——**任何单人分析得出的"地板"在被实测打破之前都是幻觉**。★

反复出现的模式：某个分析者严谨地论证出一个下界，随后另一个人（或同一批人换一条路径）用一个具体构造把它打破，**过估倍数在 4× 到 79× 之间**。典型形态：

1. 在"必须逐比特复现基线"这一前提下证明的下界 → 换用真实的**符号判定**（见 1.3）后，被认定"强制存在"的那些张量直接消失；
2. 在像素分辨率上论证的几何下界 → 换到**单元格分辨率**后整块压缩掉一个数量级；
3. 一个自证的"最便宜精确形式"下界 → 多智能体对抗式搜索用一个完全不同的代数分解把它打穿。

★强化后的规则：一个"地板"是**构建-实测**的产物，绝不是分析的结论。在你拿到一个具体构造、并且（a）真的建出来了、（b）用成本度量工具量过了、（c）通过了正确性判定门，而且（d）多路径尝试都没能打破它之前，你手上的不是地板，是一个分析估计——而经验数据表明这类估计系统性地高估 4–79 倍。★

## 0.3 唯一允许的「硬结论」= 已确认的服务端机制

以下是**规则**，不是成本极限（详见第 5 章）：

- 稀疏权重卷积 → 官方校验器的严格形状推断抛异常 → 提交作废（评分崩溃）
- 输出通道数 < 10 → 输出不是 `(1,10,30,30)` → 与固定形状的真值逐元素比较必然为假 → 0 分
- 把输出轴的索引截断到低于其必需长度 → 输出形状不匹配 → 0 分
- 输出必须是 `(1,10,30,30)`、单输入名 `input`、单输出名 `output`、静态形状

这些是你必须遵守的**格式事实**，它们**不是**"某个成本已经最低"的陈述。一条服务端规则只封死**一种编码**，永远封不死一道题。关于一道题唯一能"板上钉钉"说的只有服务端机制，绝不是成本下界。

## 0.4 停止点怎么写（中性记录，不是判决）

当你确实暂时找不到更便宜的东西时，把它写成**过程记录**：

> "已构建并实测：改写方案 A/B/C/D 以及裁剪到数据实测最大尺寸的版本。A 与 C 降低了 cost 且判定门全样本通过（保留）；B 与 D 未降低 cost 或破坏了正确性（已回滚，附原因）。在我实际构建过的所有版本中当前最优 = cost M。我看不到还有哪条更便宜的路径是我尚未构建并实测过的。"

「压到不动为止」这个说法**只允许**作为你的**目标**（"用我手上所有武器压这个编码直到它不再变小"），**绝不允许**作为对任务的**结论**（"这题不可能更小了"）。明天多一个算子技巧就可能再推动它——所以你能达到的"最好"永远是"我实际测试并度量过的一切之中最好的"，不是绝对界。

★「压不动了」是挣来的，不是宣布的★：一份停止记录只有在**你能看到的每一条更便宜的结构/原语/编码都被真的建出来、量过成本、并通过预检门**之后才成立。★禁止把一条你想到的更便宜路径记成"待办 / 未测试 / 暂时建不出来"然后收工★——那是 0.1 禁止的认输判决换了身衣服（你没说"地板"，你说的是"开放线索，未实现"）。任何"我找不到更便宜的了"必须是**在你能看到的每条路都真的构建过之后**留下的中性记录。★纸面估算（例如"那条路大概三万，跳过"）永远不能替代一次真实构建——实测值经常直接推翻估算。★

## 0.5 归零索引 —— 所有已知的"拿零分"与"自欺"陷阱（提交前逐条扫）

两类：**评测归零**（本地全过、评测端 0 分）与**假胜利**（看起来对、其实不对）。

| # | 类别 | 陷阱（一句话） | 详见 |
|---|---|---|---|
| Z1 | 评测归零 | 卷积族偏置长度 ≠ (M,)：校验器放行，运行时拒绝或未定义行为；Gemm 例外 | 1.8 |
| Z2 | 评测归零 | 校验器放行但运行时会话拒绝的算子配置（如池化 strides=[0,0] 直接崩进程） | 5.4 / 附录 |
| Z3 | 评测归零 | 结构性封禁：类型名含 "Sequence" 子串、稀疏初始化器、子图属性、输出通道<10、张量重名、函数体 | 5.1 |
| Z4 | 评测归零 | 大写禁用算子清单（循环/扫描/非零/去重/脚本/函数/压缩） | 1.2 / 5.1 |
| Z5 | 评测归零 | 存在本地全绿、评测端仍 0 分的未解个案 → 任何高风险候选一律走单换隔离提交 | 5.5 |
| Z6 | 假胜利 | **可见样本全对 ≠ 评测通过**（对于不复现基线的候选）：曾有候选可见样本全中、余量 0.998，评测端 0 分 | 1.9-C |
| Z7 | 假胜利 | 属性几何"变换"常常是**复合体**，只在一个小窗口内等价（假 rot180 实为"3×3 角块的 rot180"）——按官方样本验语义，不要按变换名字认 | 1.9-F |
| Z8 | 假胜利 | 用"通过计数"比较会放行**交易式退化**（多过 3 个、丢掉 2 个）——判据必须是**逐例超集不退化** | 1.9-B |
| Z9 | 假胜利 | 仅解码通过：argmax/解码对上真值但严格原始张量与基线不同（含输出形状不同）——不算胜利 | 1.3 / 1.4 |
| Z10 | 假胜利 | 在可见数据上拟合会过拟合（凸优化的最优解可以纯记忆）；泛化证据是**强制**的 | 1.9-C/D |
| Z11 | 假胜利 | **只用预言机标签**：绝不用"我自己对规律的理解"给合成数据打标签——这是自验证闭环陷阱 | 1.9-D |
| Z12 | 方法论 | 一个不可能性证明只杀死**它覆盖的解族**，永远杀不死**任务本身**。把证明的**适用范围**和结论一起缓存；有人做到你证明不可能的事 = 放下一切先逆向 | 1.9-G |
| Z13 | 纪律 | 滚动基线：记录现场基线的指纹与本地总分，仅当总分相对上次已知值**回退**时才停；不要钉死一个过期指纹（会误停） | 5.5 |
| Z14 | 纪律 | 尺寸裁剪只能裁到**全样本实测最大值**，绝不更紧 | 1.9-E |

---

# 第 1 章 基础：成本模型与正确性判定门

## 1.1 成本模型（必须内化）

- 单题 `score = max(1, 25 − ln(cost))`，全部任务求和 = 排行榜。判错 = 0。
- `cost = 参数总数 + 内存字节数`。**约 99% 的成本是中间激活张量的字节数。**
  - 内存 = 对**每一个节点输出 / value_info 张量**求 `元素数 × dtype 字节宽`，取其静态形状。dtype 至关重要：`fp32=4B`、`int32=4B`、`int64=8B`、`fp16=2B`、**`uint8/int8/bool=1B`（便宜 4 倍）**。
  - ★字面名为 `input` 和 `output` 的张量是**免费**的（不计入内存）。★ 整个游戏就是：让答案成为一个**终端算子的直接产物**，中间体尽量少、尽量小、尽量低位宽。
  - 参数 = 初始化器与常量节点的**元素个数**（与 dtype 无关，通常只有 10–2000 的量级）。
- **乘加运算免费。** 看起来很贵的算术，只要它避免了物化大张量，就是便宜的。
- 评测端逐元素比对**原始输出张量**（不是 argmax 解码后的标签）。

★**符号门修正（与 1.3/1.4 合读）**★：评测端真正的正确性判定不是逐元素数值容差，而是**逐通道符号解码** `(raw > 0)` 之后与 one-hot 真值做**整体相等**判断。所以"精确复现原始输出"是**保守配方**——充分但不必要。一个原始张量与基线逐元素不同（甚至在未标记单元上符号相反）的候选，只要**每个通道的符号都与真值 one-hot 一致**，就能拿满分。把更宽松的符号门当作真正的目标。

- 网格编码规则：网格 → one-hot `(1,10,30,30) float32`。格内坐标 `(r,c)` 的值 `v` 置通道 `v` 为 1；格内的 0 值置通道 0 为 1；**格外（padding）单元 = 全零向量**。小于 30×30 的答案，其答案区域之外全部是零 padding。必须精确复现这一点。
- ★关键事实 —— 排行榜 = 全量评测。★ 分数用全部测试数据算，截止后没有独立的私榜揭晓。⇒ 一个通过公开榜的候选就是通过了**完整评测**，收益**永久**不会被追回。这也是为什么本地在全部样本上的判定能逐分预测榜分。
- ★逐分精确预测★：对一个干净 one-hot 基线，本地"我的原始张量 vs 基线原始张量全样本一致"⇒ 单题替换后榜分精确移动 `ln(旧cost/新cost)`。多题合并的榜分 = 基线 + Σ 各题**精确** `ln(旧/新)`（求和用精确值，不要用四舍五入后的两位小数，会多算）。

## 1.2 图格式硬约束与禁用算子

- 单输入，名 `input`，FLOAT，**静态**形状 `(1,10,30,30)`。
- 单输出，名 `output`，FLOAT，**静态**形状 `(1,10,30,30)`，10 通道。
  ★输出必须是**完整的** `(1,10,30,30)` 画布。评测比较的是 `(raw>0)` 与完整 one-hot 真值的整体相等——它**不裁剪、不修边、不容忍画布上任何多余的正号**。裁画布 / 输出小形状 / 期望区域之外有脏值，全部是死路。（把"显示用的修剪解码器"当成评分路径是一次代价惨重的教训。）★
- 用 opset 18 构图，`ir_version = 10`，域只能是默认域 `""`。
- 不允许动态维度（出现符号维即非法）。静态形状内部的动态**取值**是允许的；动态**维度**不行。文件 ≤ 1.44 MB。不允许函数体 / 子图。
- 禁用算子：`Loop` `Scan` `NonZero` `Unique` `Script` `Function` `Compress`。★同时禁用的做法：哈希 / 查找表 / 记忆 / 任何以样本为键的表——在隐藏分布上必然判错。只用透明、通用的算子。★

## 1.3 正确性判定门 —— 标准是全程无损

★**权威判据是符号，不是逐比特一致**（普适，对干净基线同样成立）。★

"完全正确" = 你的原始输出经**忠实的评测端解码**之后逐样本等于真值（解码 == 真值，全样本 N/N）。

- "我的原始张量 vs 基线原始张量全样本一致"是一条**保守的逐比特代理**：当两边都是干净 one-hot、且你选择了去匹配原始值时，它顺带充当逐分榜分预测器；但一个干净基线**可以**被一个仅符号正确、逐比特不同的候选合法击败。
- **所有情况下的普适权威判据都是：符号解码 == 真值，全样本 N/N。**
- 没有"匹配一个有缺陷的基线"的豁免。等价性是相对**规律的正确输出**衡量的，不是相对一个本身在某些本地样本上就出错的基线。
- 不允许把工作网格裁到低于数据的真实最大尺寸。裁到**全样本（含生成样本）实测最大 H/W** 是无损的、鼓励的；再小就会丢掉被计分的样本，禁止。
- ★**成本赢 ≠ 赢**。★ 一个 `cost < 基线` 的候选在通过判定门之前不算胜利。更便宜的编码完全可能是**错的**——粗暴删偏置、破坏跨通道路由的分组切分、破坏规律的反射填充，每一个都是"更便宜且错误"。唯一的判决是"解码 == 真值，全样本 N/N"。永远报告你**实测**的 cost，不要报估算值。
- 排行榜**只**用作最终的正确性 / 服务端有效性确认，**不是搜索工具**：绝不提交不等价或裁得更小的版本然后读榜来"决定留哪个"。

## 1.4 基线表征探针（在信任任何逐比特度量之前先做）

"我的原始张量 vs 基线原始张量"这条度量**默默假设了基线的原始输出是干净的 0/1 one-hot**。很多基线不是——于是**任何**干净候选（对的错的都一样）都会是 0/N，这是结构性误判。★先测，别信任何标签★：

```python
base_raw = 基线在若干真实输入上的原始输出
is_clean = np.all(np.isin(np.round(base_raw, 3), [0, 1]))
```

基线分四个表征桶，按桶选择裁决路线：

- **CLEAN**（干净 one-hot，原始值精确落在 [0,1]）→ **路线 A**：逐比特一致 = 保守快通道 + 逐分榜分预测器。★但它**不是**这道题的权威判据★，逐比特一致充分而不必要。一个干净基线可以被一个逐比特不同、但符号解码 == 真值的候选击败。⇒ 逐比特一致 → 保守通过（便宜，用它）；逐比特不一致 → **不要否决**，落到路线 B。
- **LOGIT**（取值范围如 −20.8…+12.5，神经网络回退产物）→ 逐比特度量不可用 → **路线 B**。
- **DELTA**（`输入 + 卷积增量` 形态，被改动的单元带 2..25 量级的幅值）→ 要么精确复现增量，要么构建干净 one-hot 答案后走**路线 B**。
- **ERROR**（基线自身在受限优化级别下就崩溃，通常是某个多输入的 uint8 `Max`）→ 这是**本地工具产物**，不是硬规则。构建一个绕开崩溃算子的干净候选，走**路线 B**。

★**路线 B（评测端解码裁决 —— 与评分器逐字节等价）**★：官方比较 `user = (raw > 0.0).astype(float)`（在 0 处取阈，**不是 argmax**）与完整的 `(1,10,30,30)` one-hot 真值做整体相等。评分路径里**没有修剪**、**没有逐格颜色解码**：画布上任何一处多余的正号、或任何非 `(1,10,30,30)` 的输出形状，都是硬失败。**逐比特不一致 + 路线 B 通过 = 通过候选，不是失败。**

> ★表格标签是保守的——永远跑一次探针，不要信标签★：标注为 LOGIT/DELTA 的题，实测常常是 CLEAN。攻这类题：先跑探针确认真实路线（往往比标签更干净），以路线 B 为准。

## 1.5 三个度量的读法

- **度量一（复现基线）** = 我的原始张量 vs 基线原始张量，全部本地样本，容差 1e-3。对干净基线是保守快通道 + 逐分榜分预测器；对 LOGIT/DELTA/ERROR 不可用 → 按路线 B 判。
- **度量二（扰动 / 分布外）** = **仅供参考，不拦截**。两类已确认的分布外误判不构成阻塞：平移误判（结构锚定在固定位置，如分隔线固定在第 3 列）与重着色误判（颜色带语义绑定）。度量一 100% + 成本赢即可出货，无视度量二。
  - ★但 `0/0` 的空洞通过**不是通过**★：如果扰动生成器产出 0 个有效样本，它提供的是**零认证** → 判失败，绝不当成"没有失败就是通过"。
- **度量三（成本）** = 与当前在役版本的 cost 比较。你必须**低于**它才算赢。

## 1.6 算子死亡笔记（崩溃 / 静默错误 —— 别重新发现这些）

这些是算子**语义**事实，与运行时版本无关：

- 环绕填充：起始侧填充量超过一个周期会读取**未初始化内存**（评分时静默给出错误输出）。
- 反射填充：填充量 ≥ 维度长度 → 原生段错误（直接杀死评测进程）。
- 缩放算子在输出形状与输入形状四维全等时：走内存拷贝快路径，**roi/scales 被静默忽略**——"30×30→30×30 的单节点重映射"这种东西不存在。
- 分割算子带空字符串输出名 → 段错误；TopK / 动态量化带空名 → 非法图。
- `GatherND` / `ScatterND` 的索引**必须是 int64**（int32 能过校验器，运行时作废）。
- 服务端封禁 = 大写清单 + **算子类型名包含 "Sequence" 子串**（含该子串的算子会因此死掉）。每一个**全新**的算子族在批量使用前仍要先做一次服务端探针。

**新弹药标准流程**：

- **opset 分层**：默认 opset 12（Einsum 可用，且挤压/扩维/归约/分割的轴走属性形式 = 0 参数）；不需要 Einsum 时用 opset 10（填充/裁剪/TopK 走属性形式）；纯几何用 opset 9/4（属性形式的切片 = 零成本任意裁剪）。一个模型只用一个 opset。
- **共享初始化器**（只计费一次）：水平翻转 = 2 参数，垂直翻转 = 3，rot180 = 6。
- 阈值类算子（`ThresholdedRelu` / `Shrink`）的阈值是**属性**，是 0 成本阈值节点——用它替换"标量常量 + 比较"。
- **绝不输出池化的可选索引端口**（每处 −18000 字节）。
- 按规律形状选原语：周期 / 镜像铺贴 → 单个环绕或反射填充（成本≈6）；裁剪+缩放 / 包围盒放大 → ROI 类算子（成本 5，roi 可在图内计算）；不可分离的二维重映射 → 采样网格（≤1800）；秩 / 第 k 统计量 → 归约后接 TopK（≈47）；K 个固定单元写入 → 散射（5K 参数）。
- ★**机制预探针（在押注某个机制之前先花 5 分钟用 numpy 验；不过就作废估算）**★：
  (a) 单 Einsum 折叠可行性：规律必须是**多线性**的——选择类规律（argmax / 多数投票 / 唯一性搜索）结构上**不是**单 Einsum 可编码的。[**有界例外**：K 路众数在**符号余量足够稠密**时，可以用一个"2I−1 一致性质量"的四次读法塞进单个 Einsum。]
  (b) 折叠的控制参数必须是**静态**的——数据相关的接缝 / 枢轴 / 周期会杀死锚点迁移。
  (c) 环绕填充需要三个条件同时成立：整画布二维周期性 + 构建期固定的窗口/相位 + 固定 30×30 网格。
- ★**回退压缩标准流程（机制押注失败时不要停）**★：CP 分解 / 共享初始化器 / 穷举 Einsum 操作数顺序 / 弹药清单全扫 / 把选择逻辑标量化（TopK-2、位编码、除法搜索）。物化红线：任何全网格 fp32 中间体 ≥3600 字节、或出现补集乘积 `(1−X)`，就必须改走标量级联。
- 实测内核死亡笔记：bool 归约最大值失败；fp16 归一化链会溢出（8^9 > 2^16）；量化卷积的偏置会泄漏进 padding；填充算子**没有** `symmetric` 模式；单个仿射的缩放/ROI 算子无法表达镜像。

## 1.7 dtype 三级调色板 + 降位决策树 ★算子×dtype 可用性以本节为准★

> 前提：**本地运行时必须与评测端同版本**。同版本下，本地在"关闭全部图优化 + CPU 执行器"下直接建会话跑通，就是评测端的内核真相（同一份 CPU 内核注册表，与平台无关）。
> ★**绝不要通过成本度量脚本来探测算子可用性**——它的"剖析回退"会静默把不支持的 dtype 转掉，从而**伪造可用性**。★探针的正确做法：最小单节点模型 + 直连会话 + 官方计分器结构性复核。

★**三级调色板**（字节/元素：f32=4 → f16/u16=2 → u8/bool=1）★

**u8 层（相对 f32 省 4 倍；门槛：链上每一个节点输出在所有样本上的最大值 ≤ 255）**

- 可计算：`Where`（**仅 uint8，int8 作废**）/ `Min` / `Max`（双输入）/ `Div` / `Greater` / `Equal` / `ReduceMax` / `Clip` / `ArgMax` / `Add` / `Sub` / `Mul` / `MaxPool` / `Mod` / `BitwiseAnd` / `BitShift`
- 可搬运：`Slice` / `Concat` / `Gather` / `Transpose` / `Expand` / `Tile` / `Pad` / `GatherElements` / `GatherND` / `ScatterND`（索引**永远** int64）/ `DepthToSpace` / `Cast`
- 作废：`Einsum` / `MatMul` / `ReduceSum` / `CumSum` / `TopK` / `Trilu` / `OneHot` / `SpaceToDepth` / int8 的 `Where` / u8 索引的 `ScatterElements`
- ★u8 的加法乘法溢出会**静默按 256 取模回绕**——≤255 这道门槛是**逐节点输出**的、必须实测、绝不能假设。★

**f16 层（省 2 倍；门槛：取值为整数且 ≤ 2048——超过 2048 后 f16 的间距变成 2，不再精确）**

- **完全开放**，**包括核心收缩算子**：`Conv` / `ConvTranspose` / `MaxPool` / **`Einsum` / `MatMul`**（四者均实测整数精确，误差 0）/ `Add` / `Sub` / `Mul` / `Div` / `Min` / `Max` / `Equal` / `Greater` / `Where` / `ReduceMax` / **`ReduceSum` / `CumSum`** / `ArgMax` / `Clip` / `Pad` / `ScatterElements` / `GatherND` / `Slice` / `Concat`（`Gather` / `Transpose` / 分割类搬运算子按合法处理）。
- 真正带**小数**的张量：默认**保持 f32**。

**u16 层（省 2 倍；仅用于 >2048 的精确整数——罕见）**：`Add` / `Mul` / `Div` / `Greater` / `Equal` / `Slice` / `Concat` / `Gather` / `Transpose` / `Cast` 合法；**`Where` / `Min` / `Max` / `ReduceMax` / `Clip` / `Pad` 作废**。**i16：所有已探组合全部作废，不要用。**

★**降位决策树（每一条 f32 链都走一遍；顺序很重要——u8 的 4 倍优于 f16 的 2 倍）**★

1. 整条链都在 u8 调色板内 **且** 每个节点输出在所有样本上 ≤255 → **u8**；
2. 否则若取值为整数且 ≤2048 → **f16**（`Einsum`/`MatMul`/`ReduceSum`/`CumSum` **只有这一条**低于 f32 的路）；
3. 否则若是 >2048 的精确整数且算子在 u16 子集内 → u16；
4. 否则保持 f32。

★**边界经济学（别把省下来的钱在接缝处还回去）**★

- **尾部免费**：最后一个 `Cast(f16→f32)`，只要它的输出**就是**名为 `output` 的图输出，计费为零。
- **入口零成本**：改写一个**已存在**的 `Cast` 节点的 `to=` 属性——它的输出直接减半。
- **新加**一个边界 `Cast` 要按 元素数×字节宽 计费，只在下游收益超过它时插入；★绝不在完整 `(1,10,30,30)` 上加 `Cast`（18000 字节，自杀）★——要在裁剪后的小张量处切换 dtype。

★**永久元教训**★：版本假设是承重结构。一个错误的本地运行时版本认知，可以把一整类杠杆隐藏一整个赛季。能力问题**只能**由同版本直连会话探针（或线上确认）来定论，绝不能靠成本度量脚本跑通来定论，更不能跨版本推断。

## 1.8 属性迁移、负填充、偏置长度与负结果登记

**A. 参数杠杆 —— 旧版 opset 的属性形式迁移**

各算子转为属性形式所需的 opset 上限：切片 ≤9、TopK 的 k ≤9、填充 ≤10、裁剪 ≤10、挤压/扩维 ≤12、归约求和的轴 ≤12、分割 ≤12、归约最大/最小/均值/乘积的轴 ≤17。一张图只能有一个默认域 opset：`Einsum` 需 ≥12、`Trilu` ≥14、位运算族 ≥18——这些会把图锁死在 ≤9/≤10 形式之上。
- 现实评估：这条杠杆**真实但很小**，作为深攻小成本题时的**审计项**，不要当成一波战役来排。参数的质量大头在**收集/散射的索引**上，而索引**永远没有**属性形式——**索引生成（计算而不是存储）**才是砍参数的真正战场。

**B. 内存杠杆 —— 负填充作为免费静态裁剪（规范灰区，但有线上证据）**

- 在卷积 / 转置卷积的 `pads` **属性**上写**负值**，等于把裁剪折进卷积：零额外节点、零额外计费张量。
- 版本敏感：评测端的 onnx 容忍负 pads（约等于较旧版本的行为）；**较新版本的严格形状推断会拒绝**——本地版本必须与评测端对齐，否则会本地误杀这类模型。
- 纪律：规范灰区 ⇒ **首次使用走单换隔离确认**，此后作为标准工具。

**C. 危险 —— 卷积族的偏置长度规则（Z1）**

- 卷积 / 转置卷积的偏置，以及量化卷积的偏置（第 9 个输入，int32），**必须严格是 (M,) 形状**，M = 权重的第 0 维。**短了和长了都是违规。**
- 官方校验器（即使开启完整检查）**经常放行**它们（偏置未与 M 交叉校验）⇒ 命运取决于运行时版本：要么会话拒绝，要么越界未定义行为。这是一条已被证实的"本地通过、评测归零"机制。
- `Gemm` **豁免**——它的 C 可以合法广播，`(1,)` 的 Gemm 偏置是正确的，不要去"修"它。
- 生成器卫生：任何**通道剪枝 / 权重裁剪**的编辑，**必须同步裁剪或零扩展偏置**（分组卷积死通道收缩正是这种高危编辑形态）。
- 唯一允许的修复：把偏置零扩展到长度 M（语义保持），然后**完整重跑三验**。

**D. 方法论 —— 面向参数下界任务的符号拟合**

- 适用域：内存≈0、纯参数的小成本题（跨越低成本线的战场），此时内存工具箱已经没牌了。
- 流程：用**铰链损失**对 ±1 符号目标拟合微型卷积 / 分组深度可分 / 自门控结构；**最大化最小带符号余量**（不只是零误差）；然后整数化 / 剪枝 / 重缩放（正缩放保持 >0 的类别，负缩放会翻转），每次变异后重新验证。
- 判定注意：符号拟合候选**天然**在"复现基线"这条度量上是 0/N（它们不复现基线原始张量）。它们是**路线 B 候选**：对全部样本的精确符号契约 + 余量缓冲，然后**强制**单换隔离确认。既不要用度量一误杀，也不要用"解码看着对"误放。

**E. 战线 —— 单节点文法扫描**

- 收益数学：一个落在 cost ≤1 的命中直接赚 `+ln(旧cost)`——中位数 4–6 分。**两三个命中胜过一百次 dtype 重标。**
- 搜索形状：一个节点，图输入 `input` → 节点 → 图输出 `output`，**无初始化器 / 无常量**，静态形状；扫算子模式 × 属性 × Einsum 方程，并把输入作为**重复操作数**（自门控）。已知的满分形态之一就是 `Einsum "nchw,nkwh->nchw"`（输入自己给自己开门）：参数 0 / 内存 0 / 得分 25.0。

**F. 已登记的负结果（不要重复烧这些算力）**

- 权重线性的 `Einsum(input, 拟合权重)` 拟合：约 500 个方程模板 × 全部任务，**零**新的精确命中——只有自门控重复输入那一片区域是活的；
- 广义收集 / 置换族 `out[d,y,x] = in[perm(d), f(y), g(x)]`：完备求解器找到 28 个精确解，**每一个都在前沿成本之上或持平**——已挖尽；
- `RandomNormal(scale=0)`：崩溃运行时（断言失败）——绝不发出；
- `RandomUniform(low==high)` 当免费常量生成器：在 `cost=参数+内存` 下**最好也只是成本中性**（省 1 个参数换 ≥1 字节新计费的节点输出）——不是杠杆；
- `GivenTensorFill` / `ConstantFill`：无内核——死。

**G. 版本情报**

- 更新的运行时版本**并不等于**更兼容：某些线上被接受的算子模式，在更高版本上反而被拒。**评测端在哪个版本，本地就锁在哪个版本**；绝不在非同版本上探测能力（伪可用与伪作废都会发生）。

## 1.9 评测分布工具：映射律、评分模型、拟合教条、尺寸证明、属性几何

**A. 映射律**

任务编号与官方基准的哈希序存在确定映射（可 400/400 逐字节验证）。官方**生成器仓库**是公开的：它提供每个任务的生成器与校验器，校验器能自我复现原始的 train/test，生成器按固定种子序列采样。⇒ 评测端的隐藏样本来自**同一个生成器**——我们可以在本地采样**真实分布**。

**B. 评测模型（实验标定，本节统管所有判定）**

- 判分是**逐题全有或全无**的；
- 隐藏集是一个**固定、有界**的官方生成器样本集，在役基线在其上 100% 通过——**不是**任意重新采样。证据：某个在役模型在**任意**官方样本上只通过约 93%，却在每一次逐分匹配的提交里都拿满分；`0.93^N` 在任何合理的 N 下都排除了"任意采样"假设，重复提交又排除了"运气好碰上小 N"的变体；
- **推论 1**：在任意种子上要求绝对全通过**比评测端更严**——绝不能仅因为某个候选在任意种子样本上失败就把它误杀（或据此下调一个在役基线）；
- **推论 2 ★判据★**：对**不复现基线**的候选，提交前的判据是**逐例超集不退化**——候选必须通过基线通过的**每一个**采样样本。用**通过计数**比较是**不充分**的：它会放行交易式退化（多过 3 个、丢掉 2 个，而丢掉的那 2 个可能正好在隐藏集里）。
- **黄金判据**：候选原始张量 ≡ 基线原始张量（在官方样本上，容差 1e-3）——**函数级相等**直接继承基线的通过资格。

**C. 判例（两个范式实验）**

- **刻意记忆型探针**：可见样本全对 + 余量 0.998，线上 **0 分** ⇒ 对不复现基线的候选，可见样本全对是**必要而非充分**条件；
- **教师蒸馏实验**：以在役模型为预言机、上万官方风格样本、凸优化最优解——单层卷积**无法表达**该函数，属于**容量为负**。分类学：区分**容量为负**（违例分布均匀、分布内样本也失败）与**分布为负**（违例集中在合成层）。★**感受野预检**★：如果规律需要的上下文半径 > 2，单个小卷积就是死的——在打开任何拟合之前先做这个心算（半径 5 的稠密核 = 12,110 参数，很少划算）。

**D. 拟合教条**

标签**只能**来自预言机——已被线上验证的在役模型（教师）或官方生成器真值；**绝不**来自我们自己对规律的解读（这是自验证闭环陷阱）。任何提交前**泛化证据是强制的**：逐例超集判据是标准仪器（手工留出集是回退方案）。

**E. 尺寸裁剪教条：从赌博升级为证明**

对全部任务用官方生成器采样统计尺寸支撑集后：**绝大多数任务（约 399/400）的可见样本最大尺寸已经覆盖生成器的完整尺寸支撑集**——历史上的"尺寸赌"实践被回溯性地在分布层面证明为安全。已登记的例外：
- 个别任务的生成器输出会超过其可见最大尺寸——任何**未来**的重写必须保留更大的画布；
- 少数任务的生成器可能吐出 >30×30 的网格，而编码器上限是 30——本地采样时要跳过，不是线上风险；
- 少数生成器有拒绝采样长尾（单个种子可能跑 90 秒以上）——采样要设逐样本超时。

**F. 属性几何族（规范灰区，与运行时版本绑定）**

空间算子的几何**属性**（strides / pads / dilations / kernel）**不计费**，而且在规范之外，运行时会接受：

- **负 strides = 轴反转**（翻转、rot180）；
- **负的非对称 pads = 裁剪 + 选角**；
- **dilations=[30,30] + p=1 的 Lp 池化 = 在 one-hot 上精确拾取单个格子**（真实数据里只有一个抽头命中，其余落在零填充上）；
- k×k 的结果落在完整 `(1,10,30,30)` 画布的左上角，符号门会忽略其余的零。

已确认可达 **cost 0 / 满分 25** 的集合：翻转 / rot180 / 固定裁剪 / 单格拾取 / 每隔 k 降采样 / 转置 / 膨胀-OR。
在符号门下**每一个池化算子都是空间 OR** ⇒ **形态学膨胀类任务是单节点候选**。
rot90/270 需要一次轴交换 = 2 个节点（中间体 36,000 字节）——**不能归零**，仅作登记。

★**警告（Z7）**★：这类"变换"常常是**复合体**，只在一个小窗口内等价（"假 rot180"实际是"3×3 角块的 rot180"）。必须按官方样本验语义，不要按变换的名字认。

**G. 永久元教训（都是付过学费的）**

- 一个不可能性证明只杀死**它覆盖的解族**，永远杀不死任务本身——把证明的**适用范围**和结论一起缓存（曾有一道题被"不存在零参数 Einsum 解"排除掉，后来被属性几何拿下）；
- 一个与我们某个证明相抵触的异常（有人做到了我们证明不可能的事）是**放下一切**的信号，不是待办清单里的一项——先逆向它；
- **要模糊测试实现边界，而不只是文档化的参数空间**：在低使用频率算子的属性上塞负值 / 极值；**使用频率与规范灰区的位置反相关**（没人用的内核会跳过输入校验）；
- 滚动基线协议：记录现场基线指纹 + 本地总分，只有总分相对上次已知值回退时才停——不要钉死过期指纹。

---

# 第 2 章 主攻流程 —— 唯一主干：无损压缩死循环

一个零上下文的新窗口按顺序执行本章。**只有一条主线**：把规律吃到 100% → 从规律重写（绝不修补暴力基线）→ 建一个无损全覆盖版本 → 在死循环里压它 → 只有当你能看到的每一条更便宜路径都被构建并实测之后，才中性收尾。

## 2.1 先把规律吃到 100%

- 读**每一个**样本。任务数据含 train / test / 生成样本三组，其中**生成样本就是评测端的真实分布**。把网格 dump 成**纯文本、短行**（不要依赖绘图库）。写一个 numpy 预测器 `f(输入网格) -> 输出网格`，确认它**逐样本精确复现每一个输出**。**在假设命中 100% 之前，什么都不要建。**"看起来很明显" ≠ "能打到 100% 的精确规律"。
- **沿每一个维度剖析生成样本的分布。** 生成器变动的每一个结构旋钮（偏移、增量、模板高度、块数、周期、网格尺寸、位置、颜色），记录它**实际出现过**的完整取值集合。★任何在任何一个本地样本中出现过的取值都必须被无损处理★——没有"罕见值，跳过"这回事，生成样本是计分的。这张表给你三样东西：
  1. 真实的最大 H/W（用于无损的**实测尺寸裁剪**）；
  2. 哪些旋钮在全部数据上是**常量**（可以硬编码）、哪些是**真变量**（必须用静态形状算子无损计算）；
  3. 哪里可能存在一个**基于计数的闭式判别式**——任何这类谓词都必须在**全部**生成样本上零例外验证过，才能依赖它。

## 2.2 从规律重写，绝不修补暴力基线

- 在役基线通常是一个包罗万象的盖章器，或者一个昂贵的卷积图。**它在成本上是垃圾。忽略它的内部构造，从零设计规律本身所蕴含的最便宜网络。** 要匹配的是**目标原始输出**，不是基线的结构。
- ★"清理型"基线常常藏着两类大头，不要被它表面的算子简单度骗了★：
  1. **可删子图 / 冗余初始化器**：删掉多余的提取分支、标签填充、守卫、reshape——单题收益不大但便宜可靠。
  2. ★**规律本身允许的大幅终端折叠**★：基线的算子清单看起来只有几个收集操作，**不代表你没有思路**——从规律直接重建一个终端 Einsum，常常能带来一个数量级的折叠。
  ⇒ 攻这类基线：**先删死路和冗余，再从规律本身（2.1）重建终端收缩**，不要只是挤压既有的图。

- ★**扰动认证的不变量剪枝（高度谨慎）**★：剪掉一个语义上被要求、但在分布上**恒为空**的输入/通道（例如背景通道，当被排除的类别在整个计分集合上可证明从不触及作用区域时）。
  - ★必须**双重认证**，绝不能假设★：(1) 在**完整**计分集合上复现基线全通过；(2) **保结构扰动**（平移 / 重着色）也全部通过——关键在于这个"空"必须是**几何性的**（扰动无法制造反例，因为不变量内在于规律的几何，而不是样本巧合）。
  - ★血的教训★：一个"轨道源集合覆盖"剪枝在复现基线上全对，但保结构扰动只有约六分之一通过、随机调色板全灭 —— 说明剪枝是样本巧合而非几何性质 → 评测端该题**归零**（是 0，不是保底 1）。★把未认证的剪枝并进批次的后果：一题归零会把整批拖到低于基线。★ 这就是覆盖类剪枝必须走单换隔离、绝不进批的原因。
  - ★适用范围 = **任何从样本推导出来的界或截断**，不止通道/查找表★：样本界定的 TopK 的 k、散射计数上限、阈值/分位裁剪，全部同理。致命点是"界拟合到了已见样本、可能覆盖不了隐藏样本"，与这个界喂给哪个算子无关。★不要用"这是 TopK 不是查找表，所以本条不适用"来绕过。★

## 2.3 先建无损全覆盖版本，再在循环里压它

- **先做出一个在全部本地样本上正确的版本**——这是你的**已知正确锚点**，留着。如果后续某个压缩步骤破坏了全样本通过，回滚到它。
- ★**先拿到成本拆解**★（这样你才压对张量）：列出每一个中间张量的名字、算子、形状、dtype、元素数、字节数；单独列初始化器/常量的参数数；按字节数降序排。标出最大的空间张量、dtype 偏重的张量、重复的全画布掩码、意外的 fp32 链。诊断信号：
  - **Cast 数量及其产生的字节** 是一个诊断指标，能定位冗余的 dtype 抖动——但 Cast 消除是**清理杠杆**，不是首要目标。
  - **节点数本身就是成本信号**：高成本模型的节点数往往比低成本模型高一个数量级，常见形态是长长的 `Cast`/`Mul`/`Sub`/`Add`/`And`/`Where` 链，每一步都吐一个中间体。节点数高 = **步骤坍缩**机会。
  - 列出最大中间体的**生产者算子类型**，这样才清楚是谁在制造内存。
- 然后用第 4 章的武器进入**死循环压缩**。每拿到一个更便宜的版本，就问一次"**还能更便宜吗？**"，换下一件武器。**每改一次都要重新实测成本。**

**杠杆优先级：**

1. **结构性重写优先（收益最大）。** 用第 3 章的识别器 / 第 4 章的主招重新表达规律。
1a. ★**分段可折性诊断（必须在结构重写之后做）**★：把每个节点输出张量按内存拆开，逐个判定 **forced / foldable**——forced = 喂给不可线性化的非线性算子，或多分支重算反而更贵；其余是 foldable。**内存的下界 = Σ forced**。把每个 foldable 段折进相邻的收缩算子（重算而不存储）；对挡路的非线性段，先尝试 **one-hot 线性化**把它变成 foldable（从而把它移出强制点集合）。
    ★**但先过线性归约门**：如果它是**非线性归约级联**（前缀 OR / 后缀 OR / 池化跨度 / OR / Max），硬折通常**更贵**（实测过折叠后的成本接近折叠前的两倍）——别硬折，改用逐张量裁剪 / 量化来缩小每一个 forced 张量。★
    目标：把内存压到 ≈ Σforced，而不是停在"第一块大中间体折不动，所以收工"。
2. **标签空间坍缩**（针对全通道张量偏重的题）。★优先级信号：拆解里 `(1,10,H,W)` 全通道 one-hot 中间体越大越多，标签空间的收益越大。★ 如果规律 = 逐格标签 + 几个颜色标量，就全程在单通道 `(1,1,H,W)` 标签网格上工作，只在终端 `Equal(标签, arange10)` 处发出 10 通道 one-hot（免费）。
3. **提前空间归约。** 在昂贵逻辑**之前**把 `(1,10,H,W)` 归约成 `(1,1,H,W)`、`(10,)`、行/列边缘分布或一个标量。
4. **实测尺寸裁剪**（裁到数据真实最大值）——无损；在小画布上做重活，终端再 `Pad` 回 `(1,10,30,30)`。（要**实测**全 30 / 裁剪 / 基线三者——裁 one-hot 有时反而更贵。）
5. **步骤坍缩 / 终端发射 / 图外科。** 用一个小卷积、一次宽核池化、闭式标量算术或一个终端收集，替换长长的移位-比较-选择链。让 `output` 成为终端 `Equal`/`Gather`/`Where`/`Pad`/`Concat`/`ConvTranspose` 的产物。做公共子表达式消除、死代码消除、删尾部恒等节点、把生产者的输出名直接改成 `output`。
6. **dtype / Cast 清理放最后。** ★Cast 消除是次级清理杠杆，不是首要目标。★ 统一 dtype、消除 `Cast→算子→Cast回` 往返，大多是结构重写的**副产品**。不要为了追求"最少 Cast 数"牺牲更大的结构性胜利——实践中最大的压缩往往还会**略微增加** Cast 数量。

- ★扫描标签写着"本质需要"（很多卷积/池化）**不代表**这题不可压缩。★ 它只说明当前实现用了很多卷积/池化。被标为"本质"的高成本题依然常规性地能压掉 50% 以上。即使一个高成本的"诚实硬题"，也常常能在**表征层**（整图 dtype 处理）压缩，而规律逻辑原封不动。
- 循环里每个候选的处理：(1) 施加一个改写；(2) **真的建出 ONNX 文件**——绝不依赖估算；(3) 在判定门的会话设置下跑；(4) 过判定门（解码 == 真值，全样本）；(5) 实测成本；(6) 过第 5 章的四重预检；(7) **同时**满足"正确"与"更便宜"才保留，否则回滚该分支继续。

## 2.4 中性收尾

- 只有当你识别出的**每一条**更便宜的结构 / 原语 / 编码都已经被**真的建出来**、实测成本、过判定门、过四重预检之后，你才可以写"当前最优"这一行。**一个你看得见但没建的更便宜想法 = 你还没完成——去把它建出来。**
- ★绝不以"推理认为某事没用"作为结尾。★ 不是"这是这道谜题的本质结构"；不是"纸面估算说那条路大概三万，跳过"；不是"成本降不下去了"。**唯一的证据是一次真实构建 + 它的实测成本 + 它的判定门结果。**
- ★用**指着你建了什么**的方式陈述结论★（见 0.4）。"当前最优" = 你**实际构建并实测过**的版本中最好的那个，**不是**这道题的绝对极限。

★**可迁移到任何任务的方法总结**★

1. 用 numpy 把规律吃到 100%（逐样本全对）；剖析每一个维度的取值集合。
2. 从规律重写（忽略暴力基线）；建一个无损全覆盖版本。
3. 做成本拆解；用第 4 章武器进死循环——结构 → 标签空间 → 提前归约 → 实测尺寸裁剪 → 步骤坍缩/终端/外科 → dtype 清理；每改必测；只保留更便宜且仍全样本通过的；过四重预检。
4. 用单换隔离提交做最终确认（逐分匹配 = 服务端有效 + 收益永久落袋）。
5. 只有在每条可建路径都建过测过之后才收尾，并用中性措辞（0.4）。

---

# 第 3 章 规律识别手册（先匹配；匹配上 = 编码已知）

用 numpy 把每个识别器的预测器跑一遍全部样本；如果某个能精确复现每一个输出 → 用它的编码。`≈成本`列是这个范式**通常**能达到的量级（是压缩的瞄准点，**不是**上限）。都不匹配 → 回到 2.1 用 numpy 挖规律，再用第 4 章武器建。

## 3.1 核心变换

| # | 规律（numpy 可测） | 编码 | ≈成本 |
|---|---|---|---|
| 1 | 全局重着色：一致的值映射 `out[i,j]=f(in[i,j])` | 通道空间范式 (a) | ~600 |
| 2 | 频次秩重着色：f = 按计数排序后的颜色的反序 / 循环 ±1 | 通道空间范式 (a) | ~700 |
| 3 | 保留最频繁颜色，其余→X（或移除某色→0） | 通道选择 / 范式 (e) | ~600–9k |
| 4 | 翻转 / 转置 / rot90/180/270 | 旧路：反序收集 / 转置；**ROI 终端例外**（见 4.4）；**属性几何可归零** | 旧 ~18–22；ROI：5 参数/0 内存 → 23.39；零参算子 → 25.00 |
| 5 | 按常量 (dr,dc) 固定平移 | 切片 + 终端填充 | ~200 |
| 6 | 裁剪：输出是一块连续子网格 | 常量→切片；包围盒→定位+切片 | ~150–2.8k |
| 7 | 铺贴 / 镜像铺贴 / 旋转轨道铺贴 | 铺贴算子 / 可分离范式 (c)；旋转不可分离 → 标签空间 + 一次带预计算扁平索引置换的逐元素收集 | ~300 |
| 8 | 最近邻放大 ×k | ★转置卷积直接写到免费输出★，优于采样网格/收集 | ~40–90 |
| 9 | 分形 / 自乘积 `out=(ih²,iw²)`，块 = in 或按 in[a,b] 的谓词 | 值路线 Kronecker（见 4.10） | ~500 |
| 10 | 逐列/逐行众数广播 | 归约求和 → argmax → 广播 | ~600 |
| 11 | 保留中心/特定行列（其余为 0/padding） | 变参静态绕法 (f) + 终端收集 (b) | ~2–6k |
| 12 | 棋盘恢复 `out[i,j]=color[(i+j)%2]` | 常量 (i+j)%2 索引 → 双色收集 | ~1k |
| 13 | 周期补全（周期可变） | `idx = 锚点 + Mod(arange − 锚点, p)` 后收集（周期是**计算出的标量**，不做静态 reshape） | 2k–28k |
| 14 | 对称化 `out = where(in≠0, in, flip(in))` | `Where(in, flip)` | ~9k |
| 15 | 轴向边缘分布重着色 `out[i,j]=f(in[i,j], 行/列)` | 轴剖面范式 (d) | ~2.4k |

★**固定空间重排的成本天花板已被推翻**★：旧的 `~20–22` 上限只适用于必须物化中间内存的老路线。固定空间重排现在可以用**终端 ROI 算子**做成单节点改写（5 参数、0 内存、得分 23.39），或者用**属性几何**归零（0 参数 0 内存、得分 25.00）。

| 方法 | 参数 + 内存 | 得分 |
|---|---:|---:|
| 切片 + 填充（旧） | ≥8 参数 + 内存 | ~18–19 |
| 索引收集 | ≥3 参数 + 内存 | ~18.66 |
| **ROI 终端算子** | **5 参数，0 内存** | **23.39** |
| 零参算子（转置、属性几何） | **0 参数，0 内存** | **25.00** |

★**纪律：绝不用本手册里的理论天花板去否决线上实测结果**★——这正是本项目犯过的错误形态。线上是最终裁判；如果实测与手册的理论行冲突，**信实测，修手册**。

## 3.2 物体 / 结构识别器（结构干净时同样可以廉价编码的重规律）

- ★**射线 / 画线 / 跨度填充**★ —— 干净时无需连通域/洪水：(i) 每行/列 ≤1 个标记，(ii) 可分离（行工作 ⊥ 列工作），(iii) 目标是坐标网格的相等判断/投影。工具箱：共线标记跨度填充（逐轴前缀 OR ∧ 后缀 OR）；周期 k 射线（三重卷积 + 矩阵乘取 [颜色|奇偶|有无标记] + 异或算奇偶）；投影到结构边缘（可分离方向投影 + 不相交加法沉积）；容器开口喷流（单边开口的盒子 → 填充内部 `包围盒∩背景` + 从缺口做解析射线/扇形）。
- ★**45° 台球 / 光束反弹 → 逐行三角波**★ —— 干净时（每行 ≤1 个光束格、无平局）：单墙 `cbeam[r]=b+s·|line−b|`；双墙（周期 `2(W−1)`）`cbeam[r]=(W−1)−|((H−1−r) mod 2(W−1))−(W−1)|`。W、H 从**免费的输入**推出，用 fp16 的取模/绝对值/减法，`Equal(列索引, cbeam)` 得到光束掩码，不相交加法合成标签。
- ★**分屏 / 多面板合成**★ —— K 个共位面板（按分隔线或固定象限切分）逐格合成。模式：z 序/固定优先级；逐格布尔与/或/异或/或非；或并集→单色。从**免费的输入** one-hot 上切出每个面板的前景掩码 → uint8 布尔 / 不相交加法标签空间 → 终端相等 + 填充。便宜技巧：对**背景通道**做异或（一次相等+取反同时填好背景与命中两个通道）；前景 = ¬背景通道。
- ★**对称板 + 遮挡 → 重建**★ —— 有 D4/水平垂直对称的图被遮挡 → 用对称性重建被遮区域。**单次 180° 点反射对可恢复的摆放来说是完备的**（不需要完整八重）。用反序索引收集 + fp16 二元 Max（成员除某值外全 0 时可改用 OR 折叠）；用 1×1 卷积→唯一最大值+argmax 定位遮挡物；裁到它的包围盒或补全整图。
- ★**可分离反射 / 中心切分**★ —— 把物体沿结构线/墙/轨反射：用"存在性 + arange"读出墙的标量；反射索引 `2·轨 − arange`（**非法索引必须钳制**到一条空的 padding 行/列，否则 padding 会变脏）。双墙双像：在中线切开物体，各自给一套钳制索引，再不相交相加。
- ★**固定格架 → 逐格特征**★ —— 固定的单元格网（显式分隔线或 k×k 块阵），每个单元产出**一个**标量特征（计数/阈值/存在/中心色/多数）。单元特征用 `Sel@X@Selᵀ` 池化 / 整数矩阵乘计数 / 在**免费输入**上做跨步深度可分箱和卷积 → 阈值/相等/argmax → 小输出 → 终端相等 + 填充。
- ★**找不同**★ —— N 个全等块，输出特征与共识不同的那一个。免 argmax 的写法：共识 `M=(Σ_块 shape ≥ 2)`，`sel = ReduceMax(|shape − M|)·present`。条带可用 mod-k 陪集折叠廉价折起（无需连通域）。
- ★**整图 → 标量特征分类**★ —— 输出**完全**由一个全局标量决定（不同色数 / 总计数 / 众数 / 一个洪水布尔 / 存在集谓词）。子类型：纯 1×1 类别 id（`Gather(类别查找表)`→`Equal(arange10)`+填充）；标量 → 罐装小符号（从符号库 `Gather/Slice` + 填充）。
- ★**刚体移动 / 滑动到接触**★ —— 便宜的**前提**是每种颜色恰好一个连通分量（**必须验证**：0 个样本把一种颜色分裂成多个分量）。此时逐物体包围盒 = 逐通道归约最大值/池化的边缘分布（**无需洪水**）；闭式位移 `(dr,dc)`（滑动到接触 = 移动物与锚点包围盒在其唯一不相交轴上的间隙）；用可分离的填充+切片实现平移 + 有效性掩码；不相交加法合成标签。
- ★**单源/多源跨度填充 = 前值延续**★ —— 把源颜色跨过共线端点之间的间隙带过去：单源 = 分段矩阵乘前向填充或一次累加和；多源 = 逐行逐色前缀 OR ∧ 后缀 OR 得跨度 + 按位或。
- ★**在推导位置盖固定图案**★ —— 位置来自推导几何（中点 `Σidx·边缘分布/2`、整行×整列的交叉、唯一出现的颜色、质心）→ 用动态填充放置图案。
- ★**同心环 / 等高线绘制**★ —— 绕某中心按步长画环：`ring=Equal(Mod(D,step),0)`，`D=Max(|r−cr|,|c−cc|)`（切比雪夫）或 `|r−cr|+|c−cc|`（菱形）；`D` 是两个坐标向量的可分离外积（在终端之前都很便宜）。**免中心变体**：若某色单元本身已构成完整的环，则"到最近该色的距离变换" ≡ 环序号 ⇒ `out=C iff DT≡0 (mod p)`（省掉求解中心与相位）。
- ★**最大空矩形填充**★ —— 当网格高度有数据上界时便宜（`C(H_max,2)` 个条带）：最长空闲游程 + 结束位置用前缀最大值倍增；对扁平条带做 argmax → 4 个标量 → `Where(矩形掩码, one-hot, 输入)`。
- ★**虚线 / 格架线补全**★ —— 内部分隔线/轴 ≤1 时，最小线覆盖坍缩为一个局部可分离计数 `line_row[r] = 周长(r) OR (两条边界列之外的 1 的个数 ≥ 2)`，然后 `填充 = (水平线⊗列跨度) OR (垂直线⊗行跨度)`。
- ★**N 者中最近重着色**★ —— 每个输出格 = N 个参考中最近的那个的颜色，在**小的输出画布**上做：形态 A 距离场（坐标网格 → 方向距离 → Min 链 → 指示器 → `Where(计数==1, 颜色和, 平局)`）；形态 B 角度象限（几何切分干净时 → 一个 N×N 参考网格 + 可分离最近邻放大，更便宜，无需距离场）。
- ★**频次柱状图合成 ⇄ 柱集紧凑堆叠**★ —— 在 ≤10 宽的**颜色域**里做计数-排序-布局：逐色计数用归约求和 / `MatMul(存在性,[ones|arange])`；排序用 TopK / 两两比较；放置用行索引掩码或加法散射；终端相等。（颜色画布 ≤10 是**完全覆盖**，不是尺寸裁剪。）
- ★**二维游程去重 + 哨兵 TopK 稳定压缩**★ —— 移除连续重复的行/列：`keep[i]=fg[i] AND (第 i 行 ≠ 第 i−1 行)`（填充+切片移位后 uint8 相减 ≠0）；用 `排序键=arange+(1−keep)·1000`、`TopK(largest=0)` 得到按序的幸存源索引，非法槽位走哨兵路由 → 从哨兵填充的标签里收集。
- ★**点反射 / 对角弹出**★ —— 元素穿过盒/块中心弹到对角另一侧：逐格掩码用池化膨胀进落点块 + 非对称填充/切片平移 + 不相交加法并集（落点区域互不重叠）。
- ★**标记锚定窗口收集 → 小画布**★（盖图案的逆操作）—— 用 argmax 定位标记，`行索引=r±[..]`、`列索引=c±[..]`，可分离地收集标签 → 小窗口 → 终端相等 + 填充。**无需连通域。**
- ★**被遮挡的双周期图 → 轨道最大值重建**★ —— 周期棋盘被 0 色遮挡 → 把每个格重建为它的周期剩余类轨道上的取值（对 `相位+pc·k` 候选做 OR 折叠），裁到遮挡物的包围盒。
- ★**按大小重标连通分量**★ —— **只对小分量便宜**（直径 ≤3，且绝不能 8 邻接——要检查对角合并）：给每个前景格播种唯一 id `r·W+c+1` → 3 次（3×3 池化 + 重新掩码×前景）洪水到每个分量的根 → 对前景按根做加法散射 = 大小 → 收集回来 → 颜色映射+哨兵 → 终端相等。无交叉点的形状：`不同角朝向数 == 角数`（无需散射）。
- ★**形态学**★ 腐蚀 / 角点 / k×k 块检测 —— 先补边画布，再切片 + 按位与/或；实心 k×k = 内部全 0 **且** 4 条边带全非零（用卷积做边带）。
- ★**包围盒填充 / 矩形色图**★ —— 从行/列极值得到矩形（轴剖面 + 累加和求跨度）；互不重叠的矩形 → 对颜色轴做 `(1,10,30,1)`/`(1,10,1,30)` 边缘分布的矩阵乘 → `(30,30)` 色图；终端相等。
- ★**多朝向模板匹配**★ —— 用转置 + 反序收集造 8 个二面体核；用卷积相关来**定位**（强制 fp32 + 关闭图优化），用转置卷积来**盖章**；把朝向轴折进转置卷积的 **group** 维 + 阈值（`sum>0 ≡ 跨朝向取最大`）以替代 `group=N`+归约最大值。模板逐样本变化时，权重可以是**计算出来的张量**。
- ★**对角射线（不反弹）**★ —— 射线共享主对角 `r−c` 或反对角 `r+c`；用 `Equal(r±c, 常量)` 定位；`r−c`/`r+c` 坐标网格作为初始化器（**零激活成本**）。

> 当规律真的是内容相关的（变换依赖逐物体推理、没有任何干净结构能坍缩它），困难在于**发现规律**，而不在编码成本——继续针对全部样本挖假设（2.1）。一个你无法从数据钉到 100% 的规律，应记为"需要人工裁定线索"的阻塞项（**写清楚到底哪里有歧义、你试过什么**）——无损版本依然成立，你**理解了的那部分**的成本压缩继续进行。

---

# 第 4 章 算子武器库

> 参考手册，不是线性阅读材料。按需跳转到你要的那一族。

## 4.1 主招 —— 输出参数化坍缩

在做任何事之前先问：**"哪几个很小的参数完全决定了输出？"**

几乎每一个结构化输出都是一个**极小参数集**（逐色 / 逐行 / 逐 k 的向量或标量，或者一张标签网格）的函数，**不是**一块自由的 30×30 画布。把答案坍缩到这些参数上，用标量/小向量算子把它们算出来，然后**在终端一次性展开**成 `output`（免费）。

★**当一个小向量就够用时，绝不物化 `(1,10,30,30)` 中间体。**★

配方：推导参数向量（归约求和/最大值 → `(10,)` 的逐色统计）→ 用散射组装 → 终端 `Gather`/`Where`/`Equal`（+`Pad`）。

这是**首要杠杆**，下面所有范式都是它的特化。即使是一个完全定制的一次性几何，也应该坍缩成小参数 + 终端相等，而不是逐格画布。

★**混合输入次数 / 分段规律的前向指针**★：当规律是分段的（部分格直通、其余格按标记推导的几何掩码重着色——画连接线、在标记之间填充、盖一条路径），用 4.3 的**符号门单 Einsum 组合**把**整个**分段规律塞进**一个** Einsum 节点，而不是用多算子的"保留/绘制"流水线。那些说这类规律"需要 ≥2 个 Einsum / 不可能单 Einsum / 有 ≥X 的强制下界"的旧证明，前提都是逐比特相等；在符号门下它们坍缩成一个节点。

## 4.2 计算换空间 + ★线性归约门★

**做法**：让**一个**重收缩算子（Einsum / MatMul / Conv）直接读 `input`，把尺寸 30 / 尺寸 10 的轴收缩掉，把答案写进**尽可能小的物化中间体**（标量 / 向量 / 小矩阵）；**只在免费的 `output` 处展开成 `(1,10,30,30)`**。那个巨大的内部乘积从不被命名 / 物化，因此不计费。

★**适用条件 —— 先过这道门**★

要让"收缩坍缩空间"成立，被埋掉的那根轴（被收缩的轴）必须是**线性归约（求和 / 加权和）**——矩阵乘 / Einsum / 卷积的本质是和-积，所以内部乘积免费且可坍缩。

**非线性折叠（OR / Max / Min，或者"从一组里选一个值"这种，例如对称轨道取未被遮挡的值）不能坍缩进收缩算子**：这类的每一个被折叠项都必须**单独物化**并喂给非线性算子，收缩省不掉。

- ★实测★：把 D4 的 OR 折叠线性化成求和/计数收缩 → 成本反而暴涨且**解码全错**（除以计数==0 / fp16 边界坍缩）。
- ★多个见证★：扩张 OR、缺陷填充 OR+腐蚀 AND、逐色 MAX，**都必须保持显式**（uint8/bool 光栅或稠密卷积），**不能**强行收缩；MAX 尤其过不了这道门。
- ★这类任务已知最便宜的形式 = **显式 uint8 OR 级联**★：把遮挡物映射到 0，让按位或 = 轨道最大值（无需哨兵）；反射用 uint8 的 `Slice(step−1)+Pad`（两个 1 字节张量之和 < fp16 矩阵乘的单个 2 字节张量）。
- ⇒ **动手前先问：我的归约是线性的吗？** 线性 → 上收缩；非线性（OR/Max/Min/选值）→ 别硬折，走显式 uint8 级联。

**基础形态族**（互相组合使用）：

1. ★**读取式 Einsum（直接读 `input`）**★ —— 在收缩内部直接把值从 `input` 里拉出来，干掉每一个 `(1,10,*)` 切片 / 边缘分布。例：`Einsum('v,xyzr,bvrc->c', 值向量, 行掩码, input)` → 第 R 行的颜色值 `(30,)`，**没有切片、没有 `(1,10,1,30)` 张量**。
   - ★注意（不总是更便宜）★：把输入读进收缩会强制 Einsum 走 **fp32**（读 fp32 输入 → fp32 输出），使收缩矩阵/计数也变 fp32（2 倍）。当收缩操作数本可享受 fp16 时，"切片+转 fp16+fp16 矩阵乘"**胜过** fp32 的读取式 Einsum。⇒ 读取式只在你**不同时**携带 fp16 矩阵时才赢。
2. ★**多操作数（3 个以上）Einsum 融合**★ —— 喂进很小的向量，使权重矩阵**从不物化**：用两个 `(30,)`/`(1,1,1,30)` 操作数替代构建 `(10,30)` 的权重矩阵。Einsum 是可变元的，大小为 1 的哑轴是被接受的。
3. ★**标签空间秩 1 求和终端**★ —— 把输出写成**单个标签通道**里若干可分离秩 1 项之和，然后在一次免费的比较处展开：`Einsum('kbxr,kbxc->bxrc', P(4,1,1,30), Q(4,1,1,30)) → 标签(1,1,30,30)`（4 个 k 项 = 行模式 / 列模式 / 两个 padding 哨兵），然后 `output = Equal(标签, [0..9])`（免费的布尔 one-hot）。**任何地方都不出现 `(1,10,30,30)` 激活。** 这个 Einsum **不带通道轴**——这正是它相对通道空间 Einsum 的胜因。
4. ★**剩余类折叠（散射→收集）作为一次收缩**★ —— 周期结构 `H[c]=Σ_{j≡c mod P} row[j]`，用按 `Mod(arange,P)` 的加法散射进 P 个桶、再收集回来（**周期 P 可动态，形状保持静态**——合法）。
5. ★**免费输出原则**★ —— 只在字面名为 `output` 的张量上展开到 `(1,10,30,30)`；让终端算子（`Equal`/`Greater`/`MaxPool`/`Where`/`Pad`）成为 `output` 的**生产者** ⇒ 这次展开成本为 0。
6. ★**把多步流水线折进一次收缩**★ —— 用一个 Einsum/MatMul（常量权重，中间体全部坍缩）替换 `a→b→c→d`（每步都物化一个中间体）。

★**定位类的免计费收缩**★：要定位某个唯一的单点标记并把它的 (行,列) 喂给下游的动态切片，**不要**"切片+归约求和+argmax"。改用 `位置+偏移 = Einsum('bchw,c,h->b', input, 颜色 one-hot, 带偏移的斜坡)` 求行（求列换成 `...,w->b`）。输入的读取免费，内部的 K×H×W 面**从不计费**，输出是一个 4 字节标量。一次收缩同时折叠了：通道选择（颜色 one-hot 因子）+ 坐标加权（行/列斜坡）+ ±1 的裁剪偏移（把 `i+offset` 烘进斜坡）。整个定位阶段约 8 字节内存 + (10+H) 参数，而切窗口的找法要 224 字节以上。
**低 opset / 无 Einsum 图的姊妹武器**：带索引输出端口的池化能给出同样的全局扁平索引（`ch*H*W + row*W + col`），1 参数、约 120 字节——**但要注意 1.6 的"绝不输出池化可选索引端口"是针对不需要它的场景**，这里是刻意使用它。

## 4.3 符号门单 Einsum 组合（十个杠杆）—— 把整条混合次数的分段规律塞进一个 Einsum

**何时用**：规律是**分段 / 混合输入次数**的（保留一些格、按标记推导的几何掩码重着色另一些格——画 L 形连接、在两个标记之间填充、盖一条路径），**并且**某个逐比特分析说"这需要 ≥2 个 Einsum / 单 Einsum 不可能 / 有 ≥X 的强制下界"。那个分析的前提是原始值相等；评测端只需要**逐通道符号**匹配真值 one-hot（1.3/1.4），这就解锁了单 Einsum 宿主。

- ★**杠杆 1 — 区间谓词 = 一个二次乘积**（干掉 `[a<b]` 的次序表）★
  `(h − a)(b − h) > 0` ⟺ `h` 严格位于 a 与 b 之间，**对两种次序都成立**。标记位置因子来自免费的 `input` 读取：`(h − r_k) = Σ_i P行[i]·(h − i)`，其中 `P行[i] = Σ_w input[k,i,w]`（标记色的行投影）；相等判断 `[w == c_k] = P列[w] = Σ_r input[k,r,w]`（一维投影，无需查表）。这消灭了约 900 参数的次序表以及那些被逐比特证明当成"强制"的 f32 因子张量。

- ★**杠杆 2 — 把 `input` 当作多个独立操作数反复读**★
  Einsum 的输入列表允许 `["input","input",...,"input", <常量>]`；每一次出现都是一个**独立索引**的操作数，而 `input` 是免费的图输入（**不会重复计费**）。保守的"≤3 个数据操作数"是 LOGIT 安全通道的默认值；在符号门下可以更高。风险现在纯粹是**墙钟时间 / 内存**，不是正确性。

- ★**杠杆 3 — 被收缩的模式索引 `m`**（`Σ_m` 把多个骨架加进一个方程）★
  加一个宽度很小的 Einsum 标签 `m`（例如 4 = 保留 / 垂直 / 水平 / 角），它**不出现在输出**里，于是 Einsum 隐式对它求和。用极小的 `[m,…]` 选择子常量为每个模式激活一支、清零其余。这就是让**一个**方程能同时宿主 "保留 + 垂直 + 水平 − 角"（一组结构上**不同**的收缩单项式之和）的机制——而旧的下界证明假设这必须拆成多个 Einsum。

- ★**杠杆 4 — 占位齐次化 `occ = Σ_p X = 1`（用 one-hot 占位做次数提升）★**
  `occ[b,r,c] = Σ_p input[b,p,r,c]` 在 one-hot 域上，**格内恒为 1、padding 区恒为 0**；且 `input[b,p,r,c]·occ[b,r,c] ≡ input[b,p,r,c]`（格内）。用它把次数为 1 的"保留"项提升到与次数 ≥2 的"绘制"项相同的输入次数，使两者能住在同一个齐次多线性方程里。**额外好处**：乘 occ 会把 padding 区清零，符号门随后读作"格外所有通道 ≤ 0"→ 免费得到正确的 padding。

- ★**杠杆 5 — 运行时按书写顺序从左到右两两执行 Einsum；"门控优先"的操作数排序对正确性无影响、对性能生死攸关（普适杠杆）**★
  在**关闭全部图优化**（评测端就是这个设置）下，运行时**不搜索收缩路径**——它就按你写的顺序合并操作数。所以一个排序糟糕的方程即使在数学上完全可解，也可能内存爆炸或每样本跑几分钟。
  修法：让累加器的自由轴**由小到大**增长——(a) 先放自由轴最小的读取（产出标量或一维的）；(b) 接着放与累加器**共享轴**的操作数（这样合并不会广播）；(c) 最后放自由轴与**输出**一致的操作数（最终的小收缩）。
  ★标定见证★：同一个方程，朴素排序（多次输入读取放在最前 → 运行时先形成它们的外积）= **TB 级分配失败**；交错排序 = 十 GB 级中间体 / 每样本约两分钟；**门控优先**排序 = 峰值中间体十万级 / 每样本约一秒。**同一个方程、同一个结果、快 100 倍以上、不再爆内存。**
  ⇒ 同一个 Einsum，是"不可计分"还是"可以出货"，**纯粹取决于从左到右的操作数顺序**。
  ★验收：门控优先是**杠杆**，最大中间体 / 墙钟 / 官方计分器非空 是**筛子**——两者都要过；记录收缩路径的贪心与最优估计，如果最优路径搜索超时（≥20 秒）就**否决**这个方程。★

- ★**杠杆 6 — 整数精确分解 优于 SVD**（符号门会在核零点处杀死低秩近似）★
  在符号门下，严格为正的格必须保持 `raw > 0`，严格非正的格必须保持 `raw ≤ 0`。**SVD / 低秩近似会在核的精确零点处泄漏出微小非零值 → 该处符号翻转 → 全盘失败。只有整数精确的分解能活。**
  当你需要一个有界秩的半正定矩阵 `G`，就构造**整数分解** `G = ΦᵀΦ`，其中 `Φ ∈ {−1,0,1}`（有时 {0,1}）——这样 `G` 的每一个元素都是运行时的整数算术，逐比特精确，无浮点泄漏。
  ★实测对照★：秩 16 的整数分解**全样本通过**；带 8e-15 噪声的满秩 SVD **全样本失败**（精确零点处泄漏）。朴素的联合操作数（被分析成"下界"的那个）是五位数元素级；Φ 分解后的几何块只有几百——同一块压掉约两个数量级。
  **适用**：只要矩阵是半正定、整数低秩、且存在 {−1,0,1} 基（镜像对、对称轨道、指示器组合），就可以把 `[D,D,K]` 块替换成 `Φ[rank,D]ᵀΦ[rank,D]`。**不适用**于 `G` 含不可约非整有理元素的情况。

- ★**杠杆 7 — 用非负求和实现 OR**（两项都 ≥0 时，`A ∨ B` 坍缩成一个单项式）★
  当规律需要两项的 OR，**且两项按构造都 ≥ 0**，那么在符号门下 `SUM > 0 ⟺ OR(A,B) > 0`——两个非负项合并成**一个**单项式，**不需要**模式索引 `m`，**不需要**第二个 Einsum。
  与杠杆 3 的区别：杠杆 3 用模式索引求和**结构不同**的骨架；杠杆 7 求和的是**同骨架**的变体 `(a,b)` 与 `(b,a)`，共享多线性结构——**更紧凑**（省掉 `m` 轴）。
  ★典型★：`out[k,a,b] = X[k,a,b] ∨ X[k,b,a]`（k = 颜色）。朴素做法需要一个跨 {轴向轨道, 转置轨道} 的模式索引；杠杆 7 用两次 `input` 读取 + 通道拾取 + "占位当中性元"（杠杆 4），再用一个 `P[2,2]=[[0,1],[1,0]]` 交换 (拾取, 占位)→(占位, 拾取)，一个单项式就求和出 `X[k,a,b] + X[k,b,a]`；两者 ≥0 ⇒ 求和>0 == OR。
  ★这正是曾被"证明不可能"的构造★（那个证明假设模式索引是求和两个骨架的**唯一**方式）——参见 0.2 的地板纪律。

- ★**杠杆 8 — 单元格分辨率降采样**（结构住在格子里时，把每一个几何张量压 N×N 倍）★
  当标记 / 结构占据一个规整的 N×N **单元格**（而不是单像素），几何表就应该活在**单元格分辨率**上：`[30,30]` 的表变成 `[10,10]`（2×2 单元 + 1 宽格线）、`[6,6]`（5×5 单元）等等。用一个 `pmb[30,N_cells]` 选择子（`槽位 = 像素 // 单元宽`）把像素轴的 `input` 读取降采样到单元轴；几何原语（区间 / 恒等 / 比较）在单元分辨率上跑；结果再用 `pmb` 上投影回像素。
  ★实测★：像素分辨率的几何 `[2,30,30] = 1800`（那个被分析成"像素硬地板"的东西）→ 单元分辨率 `[2,10,10] = 200`，**该块压 9 倍**，整题压 5 倍以上。
  **不适用**：规律在单元格内部逐像素操作，或 N 逐样本变化。

- ★**杠杆 9 — 通过终端 Equal / Greater / Pad 实现免费输出**（把 `(1,10,30,30)` 的展开推迟到最后一个节点）★
  计分把名为 `input` 和 `output` 的张量排除在内存之外，所以**一个输出名为 `output` 的终端计算节点，无论形状多大，内存都免费**。三种形态：
  (i) **终端 Greater（符号解码）**：`Greater(raw_einsum, 0) → bool[1,10,30,30]` 命名为 `output`（`raw_einsum` 甚至可以是 `[1,10,1,1]`，再由 Greater+Pad 展开）；
  (ii) **终端 Equal（标签空间解码）**：`Equal(标签_uint8[1,1,30,30], arange10) → bool[1,10,30,30]` 命名为 `output`——单通道标签零内存展开成 10 通道 one-hot；
  (iii) **终端 Pad**：在小 Einsum/Greater 之后 `Pad([1,10,h小,w小]) → [1,10,30,30]` 命名为 `output`。
  ★注意★：终端节点的输出名必须**恰好**是 `output`，而且**绝不能**为 `output` 声明 value_info（见 5.2 重名归零）。布尔 dtype 没问题（符号解码与 dtype 无关）。

- ★**杠杆 10 — 通道 0 的背景边缘分布 `raw[0] = 占位 − K·覆盖`（通用的通道 0 符号正确性配方）★**
  背景是通道 0。要让 `raw[0] > 0` 在每一个背景格成立、`raw[0] ≤ 0` 在每一个被绘制的格成立，用
  `raw[0] = 占位 − K·总覆盖`，其中 `占位 = Σ_p input[b,p,r,c]`（格内 1、padding 0，杠杆 4），`总覆盖 = Σ_{绘制模式} 模式指示器[m,r,c]`，K 取一个足够大的正整数，使得抑制在被绘制的格上压过占位。
  ★K 的选择原则★：**K 必须超过 `占位` 在被绘制分支里经过所有多线性乘积之后的最大放大倍数**——如果多次读取把占位放大了 N 倍，那么 K > N·(最大覆盖)。实测见证里 K 从 4 到 1000 都出现过；**曾有一个 K=100 因为占位被多次读取放大了数百倍而失败了极少数样本，改 K=1000 才通过**。拿不准时 K=1000 是保守默认值。
  **没有这个杠杆，绘制模式很容易在被绘制的格上泄漏出 `raw[0] > 0`**——这是最常见的符号门违例。

★**架构反转（进攻性攻击类）**★
当**基线**把某种奇特 dtype 算子面（uint8 量化卷积 / int8 / 整数卷积 / 位运算）当作它的降本技巧时：**不要读、不要修基线图**——完全忽略基线图、彻底绕开它的算子面。只读任务数据的规律，从零设计一个**纯 FLOAT 的单 Einsum**，原始输出命名为 `output` + `Greater(raw, 0)` 符号解码终端（杠杆 9）。
- **为什么严格安全**：纯 FLOAT 的单 Einsum 同时绕开了本地内核缺口与评测端 dtype 接受度问题——FLOAT 在两层都普适安全。实测收益 3×–22× 的成本削减。
- **何时用（可操作的筛选规则）**：(1) 基线架构 ∈ {量化卷积流水线, 卷积流水线} 且终端算子 ∈ {量化卷积, 整数卷积, 位与} 且输出 dtype ∈ {UINT8, INT8, INT32, UINT16, UINT64}；(2) 基线成本在中段（典型 500–4000）但**不同算子种类 ≥ 8**（说明基线在靠奇特 dtype 算子而非纯收缩）；(3) 规律符合单 Einsum 的甜点形态（单实例 / 均匀成对 / 全局对称 / "忽略输入" / 固定枚举）；(4) ★跳过把基线 ONNX 当起点来读——只读任务数据，用 FLOAT 设计方程 + `Greater(raw,0)` 终端，并审计 dtype 安全性。★
- **与防御性 dtype 避坑的关系**：防御性 = "如果你的候选不小心用了某个有风险的 uint8 算子，就 Cast 成 int32/float32 修掉"；架构反转 = 高一层的**主动战略决策**，从基线架构出发决定整个设计走 FLOAT。两者**互补**，不矛盾。

★**单 Einsum 的甜点形态**★：单实例规律、固定枚举 `m ≤ 9`、简单多线性几何、小表。这是一个**必要的高投产比形态，不是充分的出货条件**——dtype 安全性可以压过形态判断。

## 4.4 终端渲染原语族（替换固定步长的转置卷积尾巴）

当基线只是把深度可分/分组的固定步长转置卷积当作**确定性的固定放大 / 盖章渲染器**、且卷积核并不携带复杂学习形状时，用下面的终端原语替换整条渲染尾巴。**共同收益：零计费中间体（内存 0），只剩极少参数。**

- ★**最大值 ROI 池化终端**★：`MaxRoiPool(input, Rois) -> output`。把 `Rois` 做成 `[1,5]` 初始化器 `[batch=0, x1, y1, x2, y2]`（覆盖有效源框），`pooled_shape=[30,30]`、`spatial_scale=1`，输出保持静态 `(1,10,30,30)`。**5 参数、0 内存。**
- ★**ROI 对齐终端**（姊妹原语）★：`RoiAlign(input, rois, batch_indices) -> output`，opset 16，属性 `mode="avg"`、`output_height=30`、`output_width=30`、`spatial_scale=1.0`、`sampling_ratio=1`、`coordinate_transformation_mode="half_pixel"`；`rois` 是 FLOAT `[1,4]` 初始化器，`batch_indices` 是 INT64 `[1]` 初始化器。**5 参数、0 内存。**
  **边界**：ROI 类算子**只做空间池化/放大**；如果那条转置卷积尾巴同时编码了带符号的通道扩张或学习到的盖章形状，**不要**用它。
- ★**打包位的 行码×列码 按位与 终端解包**★：当输出是"行位模式 AND 列位模式"的组合、而基线用量化渲染尾巴去物化它时，终端写成 `BitwiseAnd(行位, 列位) -> output`（UINT64）；`行位 = Gather(行位表, n索引, axis=0)`，行位表是 UINT64 `[16,1,30,1]` 之类的初始化器，列位保持 `[1,10,1,30]` 初始化器。标量 `n索引` 可来自很小的计数逻辑。
- ★**卷积增量 + 加法散射，叠在免费直通的输入上**★：当输出大部分是输入原样直通、再加一个很小的局部增量时，终端写成 `ScatterND(input, indices, updates, reduction="add") -> output`，`indices` 是 INT64 初始化器，`updates` 来自一条很小的切片输入路径（例如 `Conv(Slice(input,...), 增量权重, 增量偏置)`）。**这复用了图输入作为免费底板画布，只为那条小增量路径计费。**

## 4.5 检测器优先族 与 有界洪水族

★**总原则：只要有结构，优先用精确检测器；洪水只留给真正无结构的全局传播。**★

- ★**检测器优先**★ —— 先识别结构（矩形 / 盒 / 洞 / 环 / 周期 / 局部签名），用**闭式精确检测器**代替洪水。★不要把有界洪水当默认选项。★ 典型：如果连通分量其实属于矩形/盒族，**不要**跑通用洪水——用闭式矩形根编码器直接恢复根/矩形参数，而不是十几轮洪水。**先判断"分量是不是矩形、能不能闭式化"，再决定要不要走洪水。**
- ★**有界洪水 / 可达性**★ —— 当连通分量的直径小且有数据上界时，洪水**既不需要硬折叠也不需要降采样**：跑 **K 轮带掩码的池化级联**（K = 数据实测的最大分量直径），**全分辨率**，用 **uint8 存储**。射线同理（级联按数据要求的前向重复次数截断，用 bool 的填充/收集/或 存储）。
  - ★**K 截断 = 评测覆盖赌**★：K 取数据实测直径。由于榜分无公私榜切分，**线上确认之后即永久安全**（与尺寸裁剪同一套纪律）。
  - ★**规模化结论**★：有界洪水**可以**扩展到大直径（K = 直径轮即可），但 **K 轮洪水的内存就是残余地板**，替换不掉。⇒ "大直径可达性是否强制"的结论：**不是硬强制，有界洪水能解，但 K 轮的内存换不掉。** 实测把 K 往下砍会立刻掉样本，说明 K 就是数据直径的下界。
- ★**边界种子的内部裁剪解包**★ —— 边界永远是洪水种子 ⇒ 被围住的格**可证明**只可能落在严格内部（行 1..H−2、列 1..W−2）⇒ 把"位集→网格"的解包从 H×W 裁到 (H−2)×(W−2)，再把 bool 掩码 `Pad` 回 30×30、偏移 (+1,+1)。逐比特一致，干掉了解包中恒为假的外圈。
  ★**通用形态**★：任何"可证明的空间不变量 → 裁剪检测器 / 解包"都属于这一族。
- ★**大域连通分量 —— 一条记录在案的成本事实 + 原语缺口（不是判决）**★：跨大域（测地直径 ≈ 网格尺寸）的全图连通分量标注，需要 K 次迭代的池化洪水，其中间体随 K = 最长路径长度增长，于是洪水主导成本，**当前工具箱没有更便宜的大域连通分量原语**。（小分量连通、无交叉点形状、逐区域局部洪水[深度 = 区域大小] **都是便宜的**——见 3.2。）★把它记为**确切的阻塞点**（洪水 K、域尺寸）与一个**开放的原语缺口**——如果找到更便宜的大域连通分量原语，整个族都会被解锁。同时保留无损版本，并继续压那些**确实**能坍缩的部分。★

## 4.6 字节手术族（精确函数等价，不改规律）

**适用域**：射线/掩码/形态学里存在大量零填充移位与 `标签→one-hot→填充` 尾巴，且分段可折性诊断显示归约是强制的、代数折叠又被 dtype×参数税杀死。目标不是改规律，而是在保持**逐元素函数等价**的前提下做模板化的字节手术。

- ★**先切后填的移位收缩**★：零填充移位语义 `Y[r,c]=X[r−p,c−q]`（越界为 0）。把 `Pad→Slice` 换成等价的 `Slice→Pad`，缩小被计费的中间体。参数：`st=[max(0,−p), max(0,−q)]`、`en=[边−max(0,p), 边−max(0,q)]`、`pads=[0,0,max(0,p),max(0,q),0,0,max(0,−p),max(0,−q)]`。若两次移位可以直接串联到下一个移位的种子上，还能再省一笔。
- ★**哨兵填充的尾部翻转**★：基线常见尾巴是 `标签[1,1,H,W] → Equal(arange10) → one-hot BOOL[1,10,H,W]（计费 10·H·W） → Pad → output（免费）`；改写成 `标签 → Pad(哨兵) → 填充后标签 u8[1,1,30,30]（计费 900） → Equal(arange10) → output（免费）`。计费张量从 `10·H·W` 降到 900：网格越小省得越多（中等尺寸可省数千字节），30×30 时无收益。
  ★**哨兵值必须落在标签域 `{0..9}` 之外**（例如 255）。用 0 会点亮通道 0，是错的。★
- ★**共享因子的公共子表达式提取 + 初始化器去重**★：多个输出共享一个乘积因子时，把公共因子物化一次复用。相同的初始化器只保留一份。
- ★**单字节临时暂存**★：当一个 fp16/fp32 张量**只是**为了被裁剪 / 展平 / 变形（在它第一次被卷积/归约/argmax 使用之前）而存在时，就在 **uint8（1 字节）**里做那次裁剪/展平，只在链上**最小的**那个张量处 `Cast → float`。one-hot 的 {0,1} 经 uint8 往返是**逐比特精确且免费**的。
- ★**one-hot 补列省略**★：读取宽度为 K 的 one-hot 类别轴（恰有一个槽为热）时，只物化 K−1 个槽，第 K 类在量化卷积的仿射累加器里**免费**恢复为 `1 − Σ(其余槽)`。用 int32 偏置 `+1` 和 `w_zero_point=1` 编码，使存 0 的 uint8 权重表现为有效权重 −1，卷积输出把 `1 − 计数` 饱和到 {0,1}（当且仅当其余 K−1 槽都不热时点火）。**浮点读取和 uint8 转换同时缩小 1/K，被丢掉的检测器折进已有卷积（无新节点、无新张量）。**
  ★**通用**★：适用于**任何**单热类别轴的读取。
- ★**红线**★：本族必须是**精确函数等价**；随机恒等测试 + 判定门全样本通过 + 实测成本低于基线 + 官方计分器非空 + 墙钟同量级。**禁止尺寸赌，禁止引入未经验证的 算子×dtype 组合**；**不要把 numpy 参考的全对当成已构建 ONNX 的确认。**

## 4.7 编码范式表（按规律所需选择；每个都用一个**终端**算子发出 `(1,10,30,30)`）

| 范式 | 何时用 | 算子链 | ≈成本 |
|---|---|---|---|
| (a) **通道空间**重着色/选择 | 输出 = 由全局逐色统计驱动的重着色/选择 | **只**在 `(10,)` 向量上操作：归约求和（计数）→ TopK（次序）→ 造索引 → `Gather(input, idx, axis=1)` 终端。★`(10,10)` 的秩矩阵是重陷阱——待在 `(10,)` 上★ | ~600–700 |
| (b) **终端收集**装配 | 输出只有很少（≤~4）种不同的列/行**类型** | 用 `Concat` 建一个小的 `(1,10,30,K)` 源（K 种类型）；算出 `(30,)` 的列索引；`Gather(源, 列索引, axis=3)`。源用 uint8 | ~2–6k |
| (c) **可分离**重建 | 输出 = 小基块 `(1,10,k,k)` 的铺贴/扩展（铺贴/固定周期/对称） | `Gather(基块, 行索引, axis=2)` → `Gather(., 列索引, axis=3)`；行/列索引是 `(30,)`（如 `arange%周期`） | ~1.4k |
| (d) **轴剖面** | 需要的信息是逐行或逐列的 | `ReduceSum/Max(input, axis=2)`→`(1,10,1,30)`；`axis=3`→`(1,10,30,1)`。在边缘分布上组合，终端发出 | ~300 |
| (e) **标签空间** | 需要一张推导标签网格的逐格重标 | 用 **1×1 卷积** `W=[0..9]` 把 10 通道压成 `(1,1,30,30)`；在单通道上做逐格逻辑；终端 `Equal(标签, arange10)`→`(1,10,30,30)`。★绝不用 `Mul(input,系数)`——那会物化 36000★。**免费直通**：用 `ScatterElements(input, 变动通道, 更新, axis=1)` 或 `Where(掩码, 重着色, input)` 让未变通道免费通过 | ~3.6–15k |
| (f) **变参静态绕法** | 某个参数逐样本变化（中心列、周期、尺寸、裁剪、偏移） | 用**静态形状**算子计算：存在性归约最大值 → 累加和 → 相等。**动态 `Slice` 只要输出尺寸静态就合法**；只有动态**维度**非法 | — |

★**标签空间变体：行/列属性指示器 + 相等表重编码**★：如果输出只依赖"某颜色是否出现在某行/列/边界"以及少数几种标签组合，**不需要**先造完整通道的特征栈。先把 10 通道 one-hot 直接收缩成小属性（行和 / 列和 / 边列 / 填充掩码），在单通道标签网格上做 `Where/Add`，最后 `Equal(标签, 表)` 终端输出 10 通道。**关键规则**：表重编码只改变表征，**不改变** padding / 左上角对齐 / 标签可达域；先证明标签枚举与基线通道在取值上同构，再算成本。

## 4.8 dtype 杠杆与执行双路径

> 权威可用性以 1.7 为准；本节是补充的工作笔记。

- ★**按声明 dtype 计费（4 倍杠杆）**★：计分按每个张量**声明的 value_info dtype** 计费，而剖析器会把类型合法的比较/算术族输入自动转成可运行的 dtype 去**执行**。**声明 dtype 管成本；运行时内核可用性管执行。** 所以把每一个大空间掩码/网格**声明**成 **bool 或 uint8（1 字节）**——即使它以 int/float 运行，也按 1 字节计费。前提：声明的 dtype 对该算子必须是 ONNX **类型合法**的。基本上每一个标签 / 掩码 / 洪水张量都适用。
- ★**路径分裂 —— 两条执行路径，说话时永远标明你指哪条**★：
  **直连会话**（关闭全部图优化、**无**自动转型）= 判定门用的会话 = 镜像评测端；某个算子在这里没有对应 dtype 内核就会**崩溃**。
  **剖析器**（成本度量）只对 `{Add, Div, Equal, Greater, GreaterOrEqual, Less, LessOrEqual, Max, Min, Mul, Sub}` 自动转型以便测量成本。
  ⇒ **为成本而声明 dtype，但必须在直连会话上验证它真的能跑。**
- ★**把重的空间工作留在 uint8/bool；把归约/仅浮点算子保持很小；答案 = 一个终端 `Equal`/`Gather`/`Concat`/`Pad`/`Where`。**★

## 4.9 省成本原语工具箱（自由混用）

- ★**OR 折叠 = 轨道最大值**★ —— uint8 版的对称/轨道 MAX 折叠替代品：先把遮挡物/无关项映射到 0，则 `最大值 = 按位或`。无需哨兵、无需 fp16。用反序索引收集 + 转置构造轨道，再做按位或级联。
- ★**位掩码代数**★ —— 掩码是二值或互不相交时，用按位与/或/异或替代 uint8 算术掩码公式；它们保持 1 字节，且常常避免更宽的算术中间体。
- ★**mod-k 陪集折叠**★ —— 把固定的 k×k 块格架折成**一个**规范块，与朝向/计数无关，无需连通域：`折叠 = Frow @ X @ Fcol`，`Frow[a,r]=(r%k==a)`。每种颜色落在唯一一个 k 对齐的格里 ⇒ 不会混。
- ★**矩阵乘带选择 `Sel @ X @ Selᵀ`**★ —— 在**一次** fp16 矩阵乘里同时完成"可变倍率降采样"与"可变偏移的 N×N 裁剪"，绕开切片和向下取整除法：降采样带矩阵 `R[i,r]=(r≥b0+i·s)&(r<b0+(i+1)·s)`（范围比较，无需取整）；裁剪 one-hot 矩阵 `RK[i,r]=(r==k0+i)&(i<N)`。不规则带：从"存在性 → 上升沿 → 累加和带 id → 相等"构造 R。
- ★**跨步分组求和矩阵乘**★ —— `A[j,k]=(k ∈ 组 j)`；`X@A` 一次广播就把每组的和发给该组每个成员。
- ★**`BitwiseAnd(off, 2^k−1)` = `off mod 2^k`**★ —— int32 上补码安全的 2 的幂取模（不用 `Mod`、不用浮点）。用于周期为 2^k 的梳状/奇偶条纹。
- ★**实心 K×K 块检测 + 计数 = 两次卷积箱和，无需连通域/洪水**★ —— `前景箱=Conv(前景, K×K 全 1)`、`背景箱=Conv(背景, K×K 全 1)`、`分数=前景箱 − 大常数·背景箱` → argmax 选出全前景块；再加一个内部颜色箱和即可数内容物。
- ★**中心权重 > Σ 邻居 的卷积 + 相等 = 孤立像素 / 大小为 1 检测**★ —— 3×3 卷积，中心权重 > Σ(邻居权重)，则 `Equal(卷积输出, 中心权重)` 在 8 邻域为空的格上点火。无需连通域。
- ★**"相等移位"的 OR = 大块 vs 孤立点 的切分**★ —— `是主体 = 非背景 ∧ (对 8 个方向的 移位(标签)==标签 求 OR)`；`是标记 = 非背景 ∧ ¬是主体`。一次填充 + 8 次切片 + 8 次相等 + 或。把上一条的"大小 1 检测器"推广到**任意大小**的同色块，**完全不需要迭代洪水**。
- ★**值路线 Kronecker**★ —— 在**单通道值标签**里做 `kron(掩码,·)`（可分离 `kridx=off+⌊i/s⌋`、`sridx=off+i%s`），只在终端 `Equal`+`Pad` 处一次性展开成 one-hot。比铺贴 10 通道 one-hot 便宜约 1.7 倍。
- ★**逐色属性广播 = 对 `(10,)` 向量的一次收集**★ —— `属性 = Gather(属性向量, 标签网格)`。
- ★**整数卷积 / 整数矩阵乘聚合**★ —— 输入与掩码都是 uint8 时，直接在 1 字节张量上用整数卷积（或整数矩阵乘）做箱和、模板得分、局部计数；在过判定门之前把脆弱的 int32 判别链 `Cast` 走（见 5.2 的 int32 链归零陷阱）。
- ★**重标为唯一最大值来定位**★ —— 用 1×1 卷积权重（例如把标记通道的权重设得远高于其余通道），让标记成为唯一的 argmax 目标——定位它**不需要**单独的全尺寸 bool 掩码 + 浮点转换。
- ★**免"去重算子"的 top-K 抽取 + 比较交换排序网络**★ —— 不用被禁的去重/循环算子拉出连续极值：`v=ReduceMax(x)` → `Equal(x,v)`（抽取）→ `x←Where(x==v, 0, x)`（消费）；重复 K 次得到 top-K。然后用**固定的比较交换排序网络**（条件 `Where` 交换）按任意键给这 ≤K 个结果排序（携带便宜的掩码 + 标量键，其余在之后重算）。适用于**小的固定 K**。
- ★**计算出来的权重卷积**★ —— 当卷积核**由 `input` 推导**（合法的动态卷积）时，一次卷积就是积分图 / 箱和 / 模板相关 / 覆盖图（**包括可变箱尺寸**，核逐样本由 `Less(arange,h+2)⊗Less(arange,w+2)` 构造）。卷积会强制 fp16 + 关闭图优化。
- ★**旋转卷积核 = 扁平索引收集置换**★ —— 从**一份**存储的核得到 3×3 核的全部 4（或 8）个朝向：`Gather(Reshape(W,(G,9)), rot90置换, axis=1)` → `Reshape` 回 `(G,1,3,3)`——不需要转置链；与分组卷积组合即得全朝向相关。
- ★**分组卷积死通道收缩**★ —— 当卷积的活跃**输入**通道/颜色成簇时，`group=g` 使权重变成 `(Cout, Cin/g, kh, kw)`（参数 ÷ g），**仍然是稠密的**（服务端接受 group>1）。**必须保持输出通道的 logit 与基线数学等价**（活颜色 = 其组的权重；死颜色 = 基线的负偏置）。**粗暴切分是"更便宜且错误"——必须验证全样本通过。**★同时按 1.8-C 同步裁剪或零扩展偏置。★
- ★**在无用空间轴上做稠密核收缩**★ —— 如果规律的真实感受野 ⊊ k×k（例如只是水平的 1×3），就发 `(10,10,1,3)`/`(10,10,3,1)`/`(10,10,1,1)`，**仍然稠密**、服务端安全。（**从生成样本里的负例最能诊断真实感受野**。）
- ★**实测尺寸裁剪**★ —— 把输入切到数据的**真实最大网格尺寸**（全部本地样本包括生成样本上观察到的最大 H/W），所有空间工作在那里做（中间体缩小约 6–9 倍），终端 `Pad` 回 `(1,10,30,30)`。★这是**无损**的——它覆盖每一个样本。裁到**低于**数据最大值会丢掉被计分的样本 = 不无损 = 禁止。★（要**实测**全 30 / 裁剪 / 基线三者再决定——裁 one-hot 有时更贵。）
- ★**终端填充**★ —— 在小的或裁剪过的张量上完成逻辑，让 `Pad` 成为名为 `output` 的生产者（与终端相等/one-hot 发射组合），于是那个大的 `(1,10,30,30)` 结果**免费**。
- ★**用加法实现不相交掩码并集**★（不要用 `Where`/`Max`）—— 让区域**可证明互不重叠**（范围/非背景掩码），然后直接 `Add`（保持 uint8）。用于"给几个已知颜色的不相交区域上色"。
- ★**垃圾格散射 —— 无碰撞的越界路由**★ —— 当散射目标可能非法时，把坏写入送到一个备用"垃圾"槽：`idx' = Where(合法, idx, N)` 写进尺寸 N+1 的画布，然后 `Slice[0:N]` 丢掉垃圾槽。**避免了"钳制到 [0,K) 导致非法写入覆盖合法格"的 bug。**
  ★反向用法★：如果你在**全部**生成样本上验证了越界数为 0，就可以彻底删掉显式的边界掩码，纯靠特征存在性路由。
- ★**用填充+切片做整数平移**★（不要用 int32 收集）—— 按常量 (dr,dc) 平移一个 uint8 网格且**保持 uint8**（收集会强制一个 4 字节索引网格）。
- ★**一次卷积同时发出两个量**★ —— 选择权重使得 `Cast→uint8` 的截断给出字段一，而**同一个输出**的 `>t` 阈值给出字段二（例如一次卷积同时给出值标签和格内掩码）。
- ★**零成本的图外科（纯收益、零风险、逐比特一致）**★ —— 三种改写能从任何图上刮下字节：(i) 公共子表达式消除（合并算子+输入+属性完全相同的节点）；(ii) 死代码消除 + 未使用初始化器剪枝；(iii) **恒等消除**——把图的 `output` **名字**移到它的**生产算子**上，这样 `…→Identity→output` 就不再多计费一份全尺寸拷贝（`output` 是免费的）。
  ★**不要**用运行时的离线"基础/扩展"优化档去"优化"图——它会**膨胀**图（插入 fp32 转换 + 引入非法的私有域 opset）。★

## 4.10 算子缺口驱动的攻击类速查

当第 3 章的识别器匹配上了，但最便宜的已知编码仍留下很大的内存/参数残余时，缺的**通常不是思路，而是一个算子或一组基**。按症状查：

| 规律 / 基线症状 | 攻击类 |
|---|---|
| 终端把标量索引解码成 one-hot；相等/OneHot/填充+相等 太贵 | **二次抛物线解码尾**：`Concat([x, x²]) → 1×1 量化卷积`，第 k 个通道权重 `[2(k+1), −1]`、int32 偏置 `−k(k+2)`、`w_zp=1` ⇒ `1 − (x − (k+1))² > 0` 当且仅当 `x == k+1`。免 argmax、对免费输出安全 |
| 分形 / 自乘积 / ×k 自相似放大 | **深度转空间的像素重排**：把外积按通道打包 `Reshape[1,k²,1,1] + Mul` → `DepthToSpace(blocksize=k)` 把 k² 个通道像素重排进每个 k×k 块 = Kronecker 自乘积。**0 个几何参数** |
| 行/列结构化的洪水 / 包围盒 / 打包查找表，且 W ≤ dtype 位宽 | **位移打包位集工具箱**：`Conv(k=[1,W])` 用 2 的幂把一行的列打包成一个整数位域；水平邻居 = `BitShift L/R + 按位或`；垂直 = `Pad ±1 行`；限制/掩码 = 按位与；最低置位种子 = `x & −x`；补集 = 按位非。比全网格洪水便宜 |
| 画射线 / 人字纹 / 对角线 / 奇偶 / 拟合曲面 | **范德蒙坐标基渲染**（本表中出现见证最多的一类，优先内化） |
| 从矩 恢复 边长 / 跨度 / 间距 / 通道身份 | **二阶矩 / 方差几何** |
| 取存在的最高颜色 / 周期 / 胜出者 one-hot | **全局最大池化 / Hardmax 存在性** |
| 定位最稠密窗口 / 图例带 / 测试实心块 | **均值池化定位器** |
| 计数 / 游程 → 整数尺寸类别 或 温度计字形 | **量化卷积计数解码器** |
| 便宜的终端放置 / N 槽复合渲染 | **终端散射 / 渲染菜单**（最大反池化 / 整数卷积 / ROI 对齐） |
| 通过"先分类再重新生成"来修复一块被破坏的结构化板 | **哈希 / 指纹 分类-再生成** |
| 从单个加权和标量里读出 包围盒边 / 首位数字 | **对数/上取整/下取整 的位值解码** |
| 先判断是 N 种周期/情形中的哪一种，再套用对应的预计算库 | **周期 / 情形 分派与状态机** |
| 大型的计算换空间 检测器/渲染 流水线（没有单一折叠） | **重检测器/渲染**（非极大值抑制 / 密度峰值 / 移植调度） |

★**算子词表缺口登记**★：定期扫一遍**在役模型里出现过、但你的配方里从未出现过**的算子（取反、L1 归约、展平、硬 sigmoid、判 NaN 之类）。**这些是你思维盲区的直接指标**——一个规律需要它们的语义时，你会因为词表里没有而绕远路。把"展平/变形链出现在高成本模型里"当作可折叠嫌疑点主动排查。

## 4.11 砍参数战线（针对内存≈0 的纯权重题）

**定位**：对于**内存≈0 的单 Einsum / 单卷积题**，cost 全部来自权重初始化器。此时内存工具箱完全没牌——唯一的杠杆是**砍参数**。这是跨越低成本分数线（`cost ≤ 148 ⇔ score ≥ 20`）的真正杠杆。

- **下界框架**：`Σ(秩 × 轴维度)`。
- **可行性检查**：对需要保持的半正定结构做"PSD 夹逼"检查。
- **同剖面 ≠ 同构**：必须做列/行子空间检验，不能只看剖面统计相同。
- **安全类下界证明模板**：注意一个反直觉的量化关系——**物化的 f32 每元素要 4 字节内存，而存储每元素只要 1 个参数 ⇒ "算出来" 至少是 "存起来" 的 4 倍代价**。这条经常直接决定"该算还是该存"。
- **整数精确 → 跨版本比特相等** 是这条战线上数值安全性的证明方法（见 4.3 杠杆 6）。
- ★**次数纪律的澄清**★：对**数据张量**的 Einsum 操作数限制（≤3）是为了控制墙钟与内存，**不限制常量初始化器的槽位数量**——砍参数时可以自由使用多个常量操作数。

## 4.12 构图骨架（opset 18 / ir_version 10）

```python
import numpy as np, onnx, json, onnxruntime as ort
from onnx import helper, TensorProto as TP

I = [1, 10, 30, 30]
def c_(n, a, dt):
    a = np.array(a); return helper.make_tensor(n, dt, a.shape, a.flatten().tolist())
def vi(n, dt, sh):
    return helper.make_tensor_value_info(n, dt, sh)

# ★opset-18：归约求和/归约最大值/切片/填充/TopK 的 axes 与参数是【输入张量】而不是属性——要传常量★
nodes = [
    helper.make_node('ReduceSum', ['input', 'axes023'], ['counts'], keepdims=0),
    helper.make_node('Gather',    ['input', 'idx'],     ['output'], axis=1),   # 最后一个节点的输出名 = "output"
]
inits  = [c_('axes023', [0, 2, 3], TP.INT64), c_('idx', list(range(10)), TP.INT64)]
vinfos = [vi('counts', TP.FLOAT, [10])]        # 每一个中间体都要有 value_info（名字、dtype、静态形状）

g = helper.make_graph(nodes, "taskNNN",
                      [vi("input", TP.FLOAT, I)], [vi("output", TP.FLOAT, I)],
                      inits, value_info=vinfos)
m = helper.make_model(g, opset_imports=[helper.make_opsetid("", 18)])
m.ir_version = 10
onnx.save(m, "candidate.onnx")
```

★**常见构图错误**★：
- opset 18 下 归约/TopK/切片/填充 的轴与参数是**输入**，必须给常量；
- **每一个**中间体都需要一个**静态形状**的 value_info；
- **最后一个**节点的输出必须命名为 `output`；
- ★**绝不要**把 `output` 放进 `value_info`★（重名归零，见 5.2）。

---

# 第 5 章 服务端机制、四重预检与提交纪律

> 这些是关于**评测端**的规则，不是"某个成本已经最低"的陈述。一条服务端规则只封死**一种编码**，永远封不死一道题——绕开它。

## 5.1 结构性作废清单（违反 = 提交作废 / 0 分）

- ★**稀疏权重卷积 = 作废**★：官方内存计分开头就调用完整校验，其卷积形状推断在遇到稀疏浮点权重时**抛异常**，而这个异常**在服务端的 try/except 之外** ⇒ 评分**崩溃** ⇒ 提交作废。**绝不提交任何稀疏/结构化权重的卷积。** 提交前在本地跑一次完整校验（镜像服务端），发现稀疏初始化器或校验失败就跳过。**改用稠密的分组卷积/核收缩**（见 4.9，它们是稠密且服务端安全的）。
- ★**稀疏初始化器作为 Einsum 操作数 = 作废**（与上一条是**不同**的代码路径）★：一个稀疏恒等矩阵（或任何稀疏初始化器）作为 Einsum 操作数，在关闭图优化的运行时下**跑得好好的**，但官方计分器的完整校验（严格形状推断）会在评分开始**之前**就拒绝它 ⇒ 模型不可计分 ⇒ 该题 0 分。
  ★**陷阱在于**：成本度量脚本**不会**复现这个失败（它按元素数估算成本并成功跑通 ORT），所以本地成本**好看得很**——稀疏恒等只按非零元素计费（例如 30 而不是 900）——但评测端在提交时拒绝。这就是典型的"本地全绿、服务端 0 分"。★
  **两种表现形态**：(i) 形状推断报"输入秩为 0"类错误；(ii) 完整校验**直接段错误**（进程崩溃，**无法被捕获**）。两者都不可计分。
  **对策：Einsum 的操作数只用稠密初始化器。** 用 4.3 的分解技巧（共享转置基被引用多次、秩 1 外积分解、模式索引的小选择子）把稠密初始化器做便宜。
- ★**输出通道 < 10 = 0 分陷阱（本地不可见）**★：把卷积的**输出**通道缩到 `最大颜色+1`（例如 `(6,10,3,3)`）会发出 `(1,6,30,30)`；评测端比较 `(raw>0)` 与**永远是** `(1,10,30,30)` 的真值 ⇒ 形状不匹配 ⇒ 全错。而**只用于显示**的解码器会按动态通道数读，于是手工解码会显示**虚假的全对**——服务端**不做**解码。
  **铁律：任何提交之前断言 `output.shape == (1,10,30,30)`，输出通道必须 = 10。**
- ★**张量重名 → 内存计分返回 None → 0 分（本地不可见，连成本度量脚本都被骗过）**★：官方内存计分会对 `图输入 + value_info + 图输出` 做一次防御性的唯一性检查；只要**任何**张量名出现两次，它就返回 `memory = None` ⇒ 该题**无法计分** ⇒ **0 分**，无论你的网络多正确。
  最常见的触发方式：构图器为终端节点发出了一个名为 `output` 的 `value_info`——但 `图输出` 里已经声明了 `output`，于是名字**重复**。
  ★**致命之处**：成本度量脚本**不复现**这个唯一性检查，所以它会返回一个完全正常的成本、把 bug **彻底藏住**；校验器、判定门、复现基线度量也全都通过。模型在本地处处显示全对，提交后仍然 0 分。这比稀疏/通道数陷阱**更阴险**，因为它连成本度量都骗过了。★
  **对策：构图器绝不发出名为 `output` 的 value_info**（或任何与 `input`/`output` 或其它 value_info 冲突的名字）；保证每个 value_info 名唯一且与图输入/输出名不同。
- ★**int32 算子链 → 内存计分返回 None → 0 分（与上一条同类，本地不可见）**★：一条整数判别链如果**直接在 int32 张量上**跑取模/相等，可能让服务端的内存计分返回 None ⇒ 该题 0 分，而本地成本度量、校验器、直连会话**全部通过**。
  ★**对策：把离散判别链构造成 fp16 标量形式**——int32 计数 → `Cast` fp16 → 与常量做 `Equal`/`Or`——**不要**让取模/相等直接作用在 int32 张量上。★
- ★**索引截断到低于输出轴长度 = 0 分**★：收集索引短于输出轴 → 输出的高度/通道数不足 → 形状不匹配。**输出轴的索引必须保持完整长度。**
- ★**输出形状**★：必须是 `(1,10,30,30)`、名为 `output`、FLOAT、静态。（超过 30×30 的网格会被评测端/判定门/成本度量**一致忽略**——只有 ≤30 的子集被计分，所以 `(1,10,30,30)` 的画框永远够用；">30 的网格"从来不是阻塞点。）
- ★**Einsum 字母冲突 = 静默算错**★：ONNX 的 Einsum 用单字母下标；把**同一个字母**用在两个语义不同的轴上，运行时和成本度量**都不会检测到**——它们会静默计算出一个**与你意图不同**的收缩。候选跑起来不报错、产出"结果"，然后被符号门拒绝（更糟的情况是本地通过、评测端因略有差异的形状推断而失败）。
  ★**对策**：(a) 用大字母集（可用 52 个：a–z、A–Z；大方程用大小写配对）；(b) **逐字母审计每一个方程**——把操作数列表打印出来，确认每个操作数的轴标签都是你要的（本该轴无关的两个操作数之间不共享字母）。★这条在实践中只能靠**人工逐字母审计**抓出来。★

## 5.2 打包与提交纪律

- ★**打包结构致命**★：评测端期望提交压缩包**根目录**下直接是 `taskNNN.onnx`（**扁平**）。如果压缩包把它们裹在一层子目录里（`submission/taskNNN.onnx` 而不是 `taskNNN.onnx`），评测端**找不到文件**，所有题近似 0 分 → 排行榜灾难性崩塌。
  ★**已发生的实例**：一个高分压缩包因为多了一层目录前缀，被评成接近“全部任务都只拿保底 1 分”的总分。★
  ★**通用打包纪律**：每次提交前列一次压缩包目录，确认条目是根目录下的 `taskNNN.onnx`；把这项检查**写进每一个合并/打包脚本**。★
- ★**服务端处理失败的处置流程**★：整包提交返回服务端 ERROR 并点名了某些任务 →
  ① 立即**隔离**这些任务、剔除后重新合并（其余任务照常晋级）；
  ② 失败的任务**重建其算子/dtype 面**（不要原样再提一次），特别是 uint8 终端结构要**先探针**；
  ③ 重建通过之前，该任务保留旧版本，标记为服务端失败。
- ★**改变输出张量 dtype（fp16/uint8 等非 f32）有服务端回读风险**★：本地判定门与计分器可能全过，但**服务端对非 f32 输出张量的回读/校验可能失败**。
  **纪律**：任何改变输出 dtype 的任务，先做**单换隔离提交**确认服务端能回读，再进整包合并。**输出形状/通道不变是必要但不充分的——dtype 也算算子面变更。**

## 5.3 单换隔离提交 = 最终正确性确认（不是搜索工具）

- 一次只把**一道题**换进固定基线，独立提交、独立回读分数确认。**绝不累加、绝不串副本、绝不一次换多个。**
- 逐分匹配 = 文件服务端有效 + 收益**永久落袋**；它同时能抓出成本度量脚本隐藏的服务端作废（5.1 的几类）。
- 单题增益记账：`Δ = ln(旧cost / 新cost)`。多题合并的预期 = 基线 + Σ 各题**精确**值。
- **任何高风险候选**（符号拟合、规范灰区属性、改输出 dtype、样本派生的界或截断、覆盖类剪枝）**一律走单换隔离，绝不进盲批**。
- ★**滚动基线协议**★：基线在持续更新时，**记录**现场基线指纹与本地总分，**不要钉死一个过期指纹**（会误停）；只有当本地总分相对上次已知值**回退**时才停下。

## 5.4 四重预检（缺一不可 —— 一个有效的成本数字**不能**推出其余三项）

1. **成本实测**低于当前在役版本，并拆出参数 / 内存两部分；
2. **判定门**：解码 == 真值，全样本 N/N（按 1.4 选路线 A 或 B）；
3. **官方计分器返回非空**（拦截 5.1 的结构性作废）；
4. **完整校验**在本地跑通（镜像服务端；并检查无稀疏初始化器、无重名、打包结构扁平）。

## 5.5 运行时 / 构建坑

1. ★**卷积密集图的段错误（环境级）**★：很多卷积的基线在**开启**全部图优化时会崩溃。成本度量用的是**关闭**全部图优化；如果你自建会话去对比，务必显式设成关闭全部优化（单线程没问题）。**一次"崩溃"通常是这个原因，不是模型坏了。**
2. ★**padding 脏值陷阱**★：可分离收集 / 反射可能把一个 **padding 输出格**映射到一个**有前景的源格** → padding 区出现垃圾。**裁剪过的 numpy 自检抓不到它**（评测端比的是完整 30×30）。**修法：钳制每一个收集索引，使非法索引落到一条空的 padding 行/列。永远对完整 30×30 做验证。**
3. ★**无内核崩溃**★：典型是多输入的 uint8 `Max`（在关闭优化的会话下"未实现"崩溃——这也是 ERROR 桶基线的根因）。fp16 的**二元** `Max` 没问题。修法：消灭它（改用归约最大值 / Einsum / `Where`）、改用 fp16 二元 Max/Min，或（对 0/1 掩码取最大）用按位或 / `a+b−min`。
4. ★**默认优化器的"填充→池化"融合崩溃**★：如果你的判定门用的是**默认**优化档（不是关闭全部），一个独立的 `Pad`→带膨胀的 `MaxPool` 移位会被**融合**（填充折进 pads），重新制造出 `pad ≥ kernel` → 会话崩溃（而关闭优化的成本度量**把它藏起来了**）。**修法：把移位做成一次宽核池化**（pad = S−1 < kernel = S，没有可融合的东西）。**任何"池化前有填充"的构图都要在默认优化档下验证一次。**
5. ★**不要裁 one-hot —— 以及它的反例**★：当终端是到 `(1,10,30,30)` 的**通道扩张**时，工作保持**全 30**（裁 one-hot 会为一个 fp32 切片计费并让布尔终端翻倍 → 更差）。**反例**：当重活活在一个**裁剪过的单通道（标签）域**里、而全 30 会让成本 ≥ 基线时，裁到数据实测最大值就是**必须**的。**用实测决定：全 30 vs 裁到数据最大值 vs 基线，三者都量一遍。**（裁到低于数据最大值始终禁止。）

---

# 附录 A 算子 × dtype 可用性总表

> **权威判据是线上确认，不是本地想象。** 成本度量脚本为了估成本会把某些不支持的算子转成可运行的 dtype，会给出**伪可用**；反过来，一个版本不匹配的本地直连会话也会**伪拒绝**评测端实际接受的图。因此：**任何新的 算子×dtype 组合，首次使用必须走单换隔离探针。**
>
> 分级：**A** = 已确认可用；**B** = 已确认不可用 / 不采用；**C** = 上下文相关 / 先探针。同一项可以同时出现在 A 与 C（有确认过的实例，但相邻变体仍需探针）。
> 本表是**逐算子明细账**；与 1.7 的三级调色板冲突时，**以 1.7 为准**。

## A 段 —— 已确认可用

| 算子 × dtype | 备注 |
|---|---|
| `QLinearMatMul` / `QLinearConv` / `ConvInteger` | 量化与整数卷积族基本盘 |
| `Pad(uint8)` / `Pad(int8)` / `Pad(bool)` | 含**负** `Pad`（规范灰区，首用走探针） |
| `BitwiseAnd/Or/Xor(uint8)` | 位掩码代数的基础 |
| `Equal(uint8,uint8)->bool` | **终端广播 one-hot 形式**已确认可用 |
| `Equal(int8)` 终端 / `Equal(bool,bool)` | 终端形式可用 |
| `ReduceMin(uint8)` / `ReduceMax(uint8)` | |
| `ArgMax(uint8)` / `ArgMax(int8)` | 含 `select_last_index=1` |
| `TopK(fp16)` / `Div(fp16)` / `CumSum(fp16)` / `Clip(fp16)` / `Mod(fp16, fmod=1)` | fp16 层基本盘 |
| `Where(bool, fp16, fp16)` / `Where(bool, uint8, uint8)` / `Where(bool, int32, int32)` / `Where(bool, float, float)` | **四种分支 dtype 都可用** |
| `Einsum(fp16)` / `Einsum(float32)` | ★整数精确（实测误差 0）★ |
| `Greater(uint8, uint8)` + `Or(bool)` | |
| `Clip(uint8)` / `Clip(int32)`（标量） | uint8 用 `Clip` **替代**不可用的 `Min(uint8)` |
| `Slice(uint8)`，含**负步长** | |
| `Gather(uint8 数据, int32 索引)` / `GatherND(uint8 数据, int64 索引)` | |
| `ScatterND(uint8 数据, int64 索引, uint8 更新)` / `ScatterElements(uint8, int64/int32 索引)` | 索引 dtype 是硬约束 |
| `Unsqueeze(uint8)` / `Unsqueeze(bool)` | |
| `Less(int32,int32)` / `LessOrEqual(int32)` | |
| `Sqrt(float)` / `ConvTranspose(float)` | |
| `MaxRoiPool` / `RoiAlign` | 终端渲染原语（见 4.4） |

## B 段 —— 已确认不可用 / 不采用

| 算子 × dtype | 失败方式 |
|---|---|
| `Einsum(uint8/int8/int16)` / `MatMul(uint8/int8)` | **未实现**。★int8 相等的放宽**不延伸**到 Einsum/MatMul —— 收缩算子的下限仍是 fp16★ |
| `Max(uint8)` **多输入** | **未实现**，直接崩溃（ERROR 桶根因）；fp16 二元 Max 可用 |
| `Min(uint8)` | 未实现 —— 改用 `Clip` |
| `Where(bool, bool, bool)` | 未实现 —— 分支值给 fp16，或改写成算术/不相交加法 |
| `Where(int8)` | 未实现 |
| `CumSum(uint8)` | 无内核 —— 用 fp16 |
| `ReduceSum(int8)` / `ReduceSum(int16)` | 未实现（归约求和只有 int32 / fp16） |
| `ReduceMax(bool)` / `ReduceMin(bool)` | 不可用 |
| `MaxPool(bool)` | 形状推断**拒绝** —— 改用 uint8 池化（`Cast(bool→uint8)→MaxPool→Greater`） |
| `ArgMax(bool)` | 校验失败 |
| `OneHot(bool)` / `OneHot(uint8)` / `OneHot(int32→fp16)` | 无内核 |
| `Gather(uint8 索引)` / `Sum(uint8)` | 非法 |
| `TopK(uint8)` | 拒绝 |
| `Pad(uint16)` | 未实现（uint8/int32/int64/float/fp16 的 Pad 都可用） |
| `QLinearConv` 的 **int8 输出** | 未实现 |
| `ScatterElements(uint8/int16/int8 **索引**)` | 校验失败（索引类型只接受 int32/int64） |
| `ScatterElements(reduction='add')` 的 **fp16** | 无 fp16 内核 —— 直方图用 fp32 散射加（索引 int32） |
| `GatherND` / `ScatterND` 的 **int32 索引** | **索引强制 int64**（int32 过校验器、运行时作废）。省索引只能靠**减少索引张量的元素数**，不能靠降位宽 |
| **NaN 作为排序键**的 `TopK` | 次序被破坏，全样本失败 |
| 稀疏初始化器 / 类型名含 "Sequence" | 结构性作废（见 5.1） |

## C 段 —— 上下文相关 / 首用先探针

| 算子 × dtype | 说明 |
|---|---|
| uint8 比较族（`Equal` / `Greater` / `LessOrEqual`）在**图中**的非终端位置 | 曾多次出现"同一个算子在某图可用、在另一图被拒"的情况 ⇒ **上下文敏感**，首用探针 |
| `Add(uint8)` / `Sub(uint8)` / `ScatterElements(uint8)` | 部分图可用、部分不可用 |
| `Einsum(uint8)` | 已有拒绝记录 |
| 动态 `Slice→Pad` 组合 | 曾出现内存计分返回 None / 崩溃 |
| **uint8 终端** 的相等/翻转结构 | 曾触发**服务端处理错误**（模型在评测端根本跑不起来）—— 与"输出错"是不同的失败类别 |
| `Resize(uint8)`（最近邻放大）/ `Split(uint8)` / `Concat(uint8)` / `Concat(float32)` | 先探针 |
| `Cast(uint8→bool)` + `Where(bool, fp16, fp16)` | 组合形态先探针 |
| `ArgMin(uint8)` / `TopK(float32)` 双键 | 先探针 |
| `Pad(标签_u8) → Equal(arange10)` | ★成功模板★：比"先物化 10 通道 one-hot 再填充"便宜（见 4.6 哨兵尾翻转） |
| 卷积/转置卷积的**负 pads** | 规范灰区、有线上证据；本地 onnx 版本必须与评测端对齐，否则会误杀 |

---

# 附录 B 常见失败模式速查

| 症状 | 最可能的原因 | 去看 |
|---|---|---|
| 本地全对，线上该题 0 分 | 结构性作废（稀疏 / 重名 / int32 链 / 通道<10 / 打包目录）或不复现基线的候选过拟合 | 5.1, 5.2, 1.9-C |
| 复现基线度量 0/N，但解码全对 | 基线不是干净 one-hot，或候选是仅符号正确 —— **这是通过，不是失败** | 1.4 |
| 全排行榜崩塌到接近 0 | 压缩包多了一层目录前缀 | 5.2 |
| 候选跑得通但结果莫名其妙 | Einsum 字母冲突（静默算错） | 5.1 |
| 单样本要跑几分钟 / 内存爆炸 | Einsum 操作数顺序（未做门控优先排序） | 4.3 杠杆 5 |
| 符号门下大面积失败、且集中在被绘制的格 | 通道 0 的 K 常量太小 | 4.3 杠杆 10 |
| 低秩近似做完全样本失败 | SVD 在核零点泄漏 → 符号翻转 | 4.3 杠杆 6 |
| 折叠之后成本反而上升 | 归约是非线性的（OR/Max/Min/选值），不该硬折 | 4.2 线性归约门 |
| padding 区出现脏值 | 收集索引未钳制 | 5.5-2 |
| 判定门崩溃但成本度量正常 | 优化档差异（填充→池化融合 / 无内核算子） | 5.5-1,3,4 |
| 扰动度量 `0/0` | 零认证 —— **判失败**，不是"没失败即通过" | 1.5 |

---

> **最后一句，也是最重要的一句**：
> 这本手册里的每一个「上限」「典型成本」「甜点形态」都是**当下已构建并实测过的东西的记录**，不是任务的极限。
> 当实测与手册冲突时，**信实测，修手册**。
